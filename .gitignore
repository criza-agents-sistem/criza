# Credenciales — NUNCA al repo (regla de seguridad mínima, CLAUDE.md)
.env
.env.*
!.env.example

# scientific_agent/ tiene su propio repo git independiente — no se trackea acá
# (decisión pendiente de resolver: ¿submódulo? ¿fusionar historia? — ver hallazgos C5/C16/C17)
scientific_agent/

# Outputs de corridas de agentes (informes + JSON), logs de runs
outputs/
*.log

# PDFs descargados por knowledge_module.document_store (KM_DOCUMENT_STORE_DIR) — binarios, no
# se versionan; el texto extraído ya vive en el KM (Neon), esto es solo caché local
document_store_data/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
*.egg-info/

# IDE / Sistema
.vscode/
.idea/
.DS_Store
Thumbs.db
