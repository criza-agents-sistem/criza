# Progress — 2026-08-19 — Railway: el "online" de ayer no era real

## Objetivo de la sesión

Continuación directa de `docs/progress/2026-08-18.md` (Railway a mitad de camino). Sebas pidió
"continuemos" — lo que las notas de ayer daban por bueno ("online, confirmado por Sebas") no lo
estaba: `railway status`/`railway logs` (vía Railway CLI, ya logueado en la máquina, no hizo
falta esperar el conector MCP que seguía sin autorizar) mostró el deployment real en **CRASHED**,
sin dominio público generado.

## Causa raíz real (`railway logs --build` / `--deployment`)

- `ModuleNotFoundError: No module named 'docx'` — `Root Directory=api` (el fix de ayer) hace que
  Railway copie *solo* `api/` al contenedor. El `requirements.txt` de la raíz nunca estuvo
  disponible para el build.
- Más grave: `api/main.py` importa `conductor/conductor.py` y `utils/casos.py` directo vía
  `sys.path.insert` (reuso de código, ver comentario en el archivo) — con `Root Directory=api`
  esas carpetas hermanas tampoco existen dentro del contenedor. `Root Directory=api` es
  estructuralmente incompatible con este backend, no un ajuste menor.
- Intento intermedio (`api/requirements.txt` con `-r ../requirements.txt`) confirmó esto en la
  práctica: `Could not open requirements file: /requirements.txt` — `..` no lleva a ningún lado
  útil dentro del contenedor cuando el build root es `api/`.

## Fix aplicado (commits `e064967`, `8b45111`)

- `railway.json` en la raíz — `buildCommand: pip install -r requirements.txt`,
  `startCommand: cd api && uvicorn main:app --host 0.0.0.0 --port $PORT`. Reemplaza la
  autodetección de Railpack (causa del fallo original de anteayer, antes de que Root
  Directory=api "solucionara" el síntoma equivocado) por comandos explícitos — no depende de que
  Railpack adivine bien en un monorepo con Python (raíz + `market_agent/` + `scientific_agent/`)
  y Node (`web/`) mezclados.
- `api/requirements.txt` (el intento intermedio) revertido — sin efecto una vez que Root
  Directory vuelva a la raíz, no vale la pena dejarlo como setup fantasma.

## Otros 3 gaps reales encontrados en las variables de Railway

(`railway variables`, nombres solamente, sin exponer valores en el chat)

- Solo 8 de las 10 variables que las notas de ayer daban por subidas estaban realmente en el
  servicio — **faltaban `DATABASE_URL` y `ANTHROPIC_API_KEY`**. Sin `DATABASE_URL` la API no
  hubiera arrancado ni arreglando el resto. Cargadas ahora desde `api/.env` local (vía
  `railway variables --set`, el valor nunca apareció en el texto de los comandos — command
  substitution en el propio shell).
- `API_AUTH_PASSWORD` tenía el placeholder literal `"elegí-una-contraseña-acá"` sin completar
  nunca — reemplazada por una contraseña real generada (`secrets.token_urlsafe`), pasada a Sebas
  fuera de cualquier archivo (mostrada una vez en el chat, borrada del scratchpad temporal
  después de leerla).
- Dominio público generado (`railway domain`): `https://criza-production.up.railway.app`.

## De paso

`vercel link` re-linkeó el proyecto `web/` (necesario para poder leer/pushear env vars por CLI) y
agregó `.vercel` + `.env*` a `web/.gitignore` (evita trackear `.env.local`, que quedó con un
`VERCEL_OIDC_TOKEN` real). Confirmado con `vercel env pull` que `NEXT_PUBLIC_API_URL` y
`SITE_AUTH_USER`/`PASSWORD` en Vercel están **vacíos** todavía (no es fuga de secreto — son
placeholders sin valor real puesto, confirmado además por curl directo a
`https://criza-chi.vercel.app/` devolviendo 401 con el `WWW-Authenticate: Basic realm="CRIZA"` de
`proxy.ts`, así que el auth de la app sí está activo en producción con otros valores que sí están
cargados).

## Bloqueador real para cerrar esto — un solo paso manual

El Railway CLI (v5.23.1, esta sesión) no expone "Root Directory" por ningún subcomando
(`service`, `config` pide un SDK de TypeScript que no está publicado en npm todavía — se probó,
404). Sebas tiene que entrar al dashboard de Railway → proyecto `scintillating-intuition` →
servicio `criza` → Settings → Build → borrar "Root Directory" (`api` → vacío) → Save. Recién ahí
`railway.json` (ya en el repo) entra en efecto.

## Cierre de sesión — registro en el KM

Decisión `componente=infra` registrada vía `scripts/_registrar_decision_etapa19_railway_fix.py`
(`scripts/km_decisiones.registrar_decision`) y `agents.md` regenerado con
`scripts/generar_agents_md.py` — refleja que el "online" de ayer no era real y el diagnóstico
completo de hoy.

## Primer paso de la próxima sesión, en orden

1. Confirmar que Sebas cambió Root Directory en el dashboard.
2. `railway redeploy` (o push trivial) y seguir `railway logs --build`/`--deployment` en vivo.
3. Si levanta bien: `curl https://criza-production.up.railway.app/health` (o el path de health
   real que tenga `api/main.py`).
4. Actualizar `NEXT_PUBLIC_API_URL` en Vercel (`vercel env` — ya se puede, el proyecto está
   linkeado) con esa URL, redeploy del frontend.
5. Prueba real de punta a punta: login en `criza-chi.vercel.app` con `SITE_AUTH_USER/PASSWORD`,
   conversación real con el Conductor que efectivamente llegue al backend en Railway.
6. Pendiente aparte, sin tocar todavía (anotado, no resuelto): `orquestador/invocador.py:72`
   arma el título de cada `documento_caso` como `f"Evaluación — {spec.nombre}"` — fijo, no
   distingue cuando el mismo agente corre dos veces con foco distinto (el gap de "6 vs 4
   documentos" de la sesión del 2026-08-18). Necesita una decisión de diseño (de dónde sale el
   texto distintivo del título — ¿del objetivo/consigna que arma el Conductor al invocar?
   ¿resumen auto del contenido?) antes de tocar código — no se implementó nada todavía.
