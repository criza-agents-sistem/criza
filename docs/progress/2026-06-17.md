# Sesión 2026-06-17

## Qué se hizo

### Acceso INTA — diagnóstico y contacto (SEB-151)

**Diagnóstico técnico:**
- `repositorio.inta.gob.ar` — página principal carga (DSpace sobre CDN). Sub-rutas (discover, REST API, OAI-PMH) dan connection closed — el backend Java sigue con problemas aunque el front-end estático responde.
- `repositoriosdigitales.mincyt.gob.ar` (SNRD) — funcional. Contiene producción INTA real. RSS disponible (`&view=rss`). OAI-PMH en SNRD: 404. Filtro `institution:INTA` en VuFind no funciona correctamente; buscar "inta + tema" sí devuelve resultados INTA.

**Contacto establecido:**
Gisela (bibliotecaria INTA) se puso en contacto. Ofrece:
- Búsquedas manuales con lenguaje natural + controlado
- PDFs de material no disponible en acceso abierto
- Sesión conjunta de búsqueda

**Mensaje enviado a Gisela:**
Se redactó y envió mensaje consultando:
1. Endpoint OAI-PMH y posible whitelist de IP (justificado como sistema de inteligencia tecnológica para transferencia biotech → industria, beneficio para INTA = visibilidad y citación)
2. Vocabulario controlado utilizado (¿AGROVOC?)

**Estado:** esperando respuesta de Gisela.

**Próximo paso según respuesta:**
- Si OAI-PMH disponible → buildear tool `search_inta` usando OAI (full metadata + texto completo eventual)
- Si no → tool `search_inta` vía SNRD/RSS (búsqueda por "inta + tema", parseo XML)
- Ambas opciones integran a `evidence_generalista` como fuente local

---

## Qué quedó pendiente

- Corrida end-to-end del pipeline (Orquestador → market → evidence → armador) — postergada hasta tener respuesta de Gisela sobre INTA + aclarar que el pipeline arranca con un pedido (dolor/investigacion), no con un oportunidad_id preexistente
- SEB-148 (Repotenciar Market Agent) — In Progress, sin cambios esta sesión

## Próxima sesión sugerida

Cuando llegue respuesta de Gisela:
1. Buildear tool `search_inta` según el camino que habilite
2. Integrar a `evidence_generalista`
3. Hacer la corrida end-to-end con el pipeline completo
