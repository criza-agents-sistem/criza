# Sesión 2026-06-30

## Contexto

Continuación de la sesión 2026-06-29. Se retomó con el backfill de tipos en background corriendo.

---

## Trabajo realizado

### 1. Backfill de `documento.tipo` — 278 docs corregidos

El background task completó exitosamente. Resultados confirmados:

| Cambio | Cantidad |
|---|---|
| paper → ponencia | 168 |
| paper → tesis | 91 |
| paper → parte_libro | 8 |
| paper → libro | 4 |
| paper → divulgacion | 4 |
| reporte → libro | 1 |
| reporte → parte_libro | 1 |
| reporte → folleto | 1 |
| **Total actualizados** | **278** |
| Sin cambio | 1.362 |
| No encontrados | 0 |

Script: `criza/ingest/backfill_tipos_inta.py`

### 2. Verificación de tests

- `evidence_generalista`: 33/34 pasan. El único fallo (`test_run_agent_caso_real`) es pre-existente — integration test con UUID placeholder inválido. Sin regresiones por el enum expandido.
- `investigacion_amplia`: 11/18 pasan. Los 7 fallos (`test_run_contract_*`) son pre-existentes — usan `asyncio.get_event_loop()` que Python 3.13 no acepta sin loop activo. Sin regresiones.

### 3. Actualización de agents.md

- Harvest INTA v0.1 → v0.2: documentada la taxonomía corregida con distribución post-backfill
- Download PDFs: conteo corregido ~551 → ~984

---

## Estado del corpus INTA CICVyA post-sesiones 2026-06-29/30

| Tipo | Cantidad | Fuente de clasificación |
|---|---|---|
| paper (artículos científicos) | ~1.088 | COLECCION_TIPO + fallback dc:type="article" |
| ponencia (congresos) | 168 | COLECCION_TIPO col_*_933, 5334, 6346, 9835 |
| tesis | 91 | COLECCION_TIPO col_*_2078, 3588–3591 |
| reporte (informes técnicos) | ~168 | COLECCION_TIPO col_*_551, 21744 |
| parte_libro | 9 | COLECCION_TIPO col_*_3736 |
| libro | 5 | COLECCION_TIPO col_*_811, 1968 |
| divulgacion | 4 | COLECCION_TIPO col_*_6378, 6381 |
| folleto | 1 | COLECCION_TIPO col_*_22363 |
| **Total** | **~1.641** | |

Docs con texto completo: ~984 (de 1.641; ~610 sin PDF público en DSpace).

---

## Fixes aplicados en la sesión anterior (2026-06-29)

_(Documentado acá para completar el registro, el código ya está mergeado)_

1. **`criza/utils/inta.py` — bug `institutos`**: `s.attrib.get("setSpec","")` → `s.text`. Los valores de setSpec son texto del elemento, no atributos.
2. **`criza/ingest/harvest_inta.py` — taxonomía**: `TIPO_MAP` (5 entradas keyword) reemplazado por `COLECCION_TIPO` (24 mapeos exactos col_ID→tipo) + `TIPO_MAP` como fallback.
3. **`knowledge_module/migrations/004_tipo_documento_inta.sql`**: expandido CHECK constraint con 6 tipos nuevos: tesis, ponencia, libro, parte_libro, divulgacion, folleto.
4. **Enums de agentes**: `search_corpus_inta` en `evidence_generalista.py` e `investigacion_amplia.py` expandidos con los 6 tipos nuevos.
5. **`criza/ingest/download_pdfs.py` — encoding**: `_sanitize()` elimina null bytes y surrogates antes del INSERT a PostgreSQL.

---

## Pendientes carry-forward

- [ ] Tests asyncio loop en `investigacion_amplia` (`test_run_contract_*`) — pre-existente, no urgente
- [ ] Pilot Gisela INTA (bibliotecaria): 15-20 docs acceso cerrado para probar el proceso
- [ ] SEB-149: Ampliar Agente Científico → Evidencia Científica
- [ ] SEB-147: Agente Descubrimiento de Demanda (divergente redefinido)
- [ ] Validar pipeline end-to-end con `pipeline_dolor.yaml` y un caso real

---

## Próxima sesión sugerida (post-mañana — actualizado al final de sesión)

Ver sección "Sesión 2" abajo.

---

## Sesión 2 — Planificación interfaces CRIZA (web + bot)

### Decisiones cerradas

**Qué se construye:**
- Canal 1: **Bot Telegram** → Orquestador/Motor (solo Sebas por ahora)
- Canal 2: **Web app** (acceso externo, equipo CRIZA — Sebas, Pablo, Andrés)

**Stack web (definitivo, no para la reunión):**

| Capa | Tecnología | Hosting |
|---|---|---|
| Frontend | Next.js / TypeScript | Vercel (proyecto `criza`) |
| Backend + Bot | FastAPI Python | Railway (proyecto `criza`) |
| DB | Neon existente (`criza`) | — |
| Auth | NextAuth + JWT (mismo que Conflur) | — |

El bot Telegram vive como webhook route dentro del FastAPI — un solo servicio en Railway.

**Auth:** NextAuth + JWT. FastAPI verifica el JWT en cada request. Sin WebAuthn por ahora (herramienta operativa, no producto de cliente). Mismo patrón que Conflur (D11 — estándar de plataforma).

**Seguridad:** Nivel 1 + 2 (usuarios con login, sin multi-tenancy — toda la data de CRIZA es compartida entre los miembros del equipo).

### Secuencia de construcción (orden óptimo por dependencias)

1. **Caso real ganadería bovina** — valida el motor antes de construir interfaces encima
2. **Design Gate `criza/api/`** — obligatorio antes de código (CLAUDE.md)
3. **Design Gate `criza/web/`** — obligatorio antes de código (CLAUDE.md)
4. **FastAPI backend + webhook Telegram** — API endpoints + bot en un servicio
5. **Next.js frontend** — sobre el backend funcionando, sin mocks
6. **Deploy Railway + Vercel**

### Issues Linear creados

- SEB-XXX (épico) — Interfaces CRIZA: web + Telegram
- Sub-issues: caso real · Design Gates · FastAPI · Next.js · Deploy

---

## Próxima sesión sugerida (definitivo)

**Correr pipeline_sector.yaml con "ganadería bovina"** — primer paso de toda la secuencia.
