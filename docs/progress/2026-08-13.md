# Progress — 2026-08-13 — Independizar CRIZA de EMPRESAS-IA + instalar knowledge_module por pip

## Objetivo de la sesión

Dos cosas, pedidas juntas: (1) sacar `criza/` físicamente del árbol de `EMPRESAS-IA/` (mismo
patrón ya usado con `eia1` → `Conflur/`), y (2) conectar `criza/` para que instale
`knowledge_module` como paquete pip real, en vez de depender de que sea carpeta hermana —
pendiente explícito dejado por la sesión de separación del KM (2026-07-24, ver
`docs/progress/2026-07-24.md`).

Plan completo en `C:\Users\sebab\.claude\plans\cuddly-sauteeing-snowglobe.md`.

## 1. Cabo suelto cerrado — repo raíz EMPRESAS-IA

El repo raíz tenía sin commitear la separación del KM (`.gitignore`, `CLAUDE.md`, hallazgo C25,
deletions de `knowledge_module/*`). Commiteado (`b4d50ad`) antes de apilar el trabajo de hoy
encima de un estado sucio. De paso: `.idea/` (JetBrains) agregado a `.gitignore` del repo raíz,
no estaba.

## 2. `criza/` autosuficiente (todavía en su ubicación actual — el move físico queda para después)

- **`.env` propio de `criza/`** (`DATABASE_URL`, `EMBEDDING_PROVIDER/MODEL/DIM`, `BGEM3_URL`,
  `ANTHROPIC_API_KEY`) copiado de `knowledge_module/.env` — 12 scripts dejan de alcanzar
  `knowledge_module/.env` por path relativo (`ingest/harvest_inta.py`, `ingest/download_pdfs.py`,
  `ingest/chunk_corpus.py`, `ingest/backfill_tipos_inta.py`,
  `ingest/backfill_inta_texto_completo.py`, `ingest/migrate_inta_to_corpus.py`,
  `ingest_corrida.py`, `ingest_historico.py`, `migrate_documento_v03.py`,
  `migrate_fts_documento.py`, `orquestador/run_dolor.py`, `orquestador/run_sector.py`).
  `ingest_corrida.py` además perdió el fallback muerto que buscaba
  `criza/divergent_agent/.env` (carpeta borrada hace rato). `.env.example` nuevo en la raíz
  documenta el set completo sin secretos.
- **4 migraciones INTA/CRIZA** (`001_v02_tenant_documento.sql`, `002_reembed_bgem3.py`,
  `004_tipo_documento_inta.sql`, `005_tipo_ficha_oportunidad.sql`) — no viajaron al paquete del KM
  por ser específicas de instancia, vivían sin trackear en ningún repo. Ahora en
  `criza/migrations/` con README explicando que ya están aplicadas al Neon real, no re-ejecutar.
- **`auditor_registry.yaml`** perdió el prefijo `criza/` en las 8 ocurrencias — con CRIZA como su
  propio repo, el root del auditor pasa a ser el propio `criza/`, no `EMPRESAS-IA/`.
- **Comando del auditor** en `agents.md` actualizado: `python -m knowledge_module.auditor
  --registry auditor_registry.yaml --root .` (antes `cd knowledge_module && python -m auditor
  --registry ../criza/auditor_registry.yaml --root ..` — asumía el layout viejo pre-`src/`).
- **Referencias de path `../` a `EMPRESAS-IA/`** corregidas en `agents.md` y
  `docs/architecture.md` (ya no hay `..` válido entre dos repos independientes). El árbol ASCII de
  `agents.md` se ajustó solo en el nivel superior (para no mostrar `criza/` como subcarpeta) — no
  se resincronizó entero, ya estaba desactualizado de antes (`divergent_agent/`,
  `convergent_agent/` ya borrados).

## 3. Instalación real + verificación

`pip install -e ../knowledge_module` (todavía válido en la ubicación actual). Verificado real,
no mock:
- `from knowledge_module.motor import api as motor_api` importa sin error.
- `motor_api.buscar(area="corpus_cientifico", consulta="biotecnologia", tenant="criza", limit=3)`
  devuelve 3 resultados reales del Neon de CRIZA.
- `orquestador/tests`: **28/28** verde.
- `market_agent/tests`: **72/73** verde.
- `evidence_generalista/tests`: **48/49** verde.
- `investigacion_amplia/tests`: **39/40** verde.

## 4. Hallazgos preexistentes, NO causados por este cambio (no se tocaron — fuera de alcance)

Confirmado que ninguno de estos 4 es una regresión de hoy: no toqué ninguno de los archivos
involucrados, y el contenido de `.env` es idéntico al que ya se usaba (solo cambió desde dónde se
carga).

1. **`armador/armador.py:153`** referencia `_CRIZA_DIR`, nunca definida en ese archivo (tampoco
   `_ARMADOR_DIR` en `armador/run.py:27`) — `NameError` que bloquea la colección de **toda**
   `armador/tests` (31 tests documentados en `agents.md` como verdes, en realidad no corren).
2. **`tests/test_ingest.py:148`** — `IndentationError`, bloquea la colección de esa suite.
3. **`market_agent/tests/test_market_agent_v1.py:441`** e
   **`investigacion_amplia/tests/test_investigacion_amplia.py:532`** — `from preflight import
   ...` (import viejo, pre-empaquetado del KM) en vez de `from knowledge_module.preflight import
   ...`, que ya es lo que usa el código real de los 3 agentes. Quedó desactualizado solo en esos
   2 tests.
4. **`km_tools/tests`**: 6/28 verde (22 fallos) — y **`utils/tests`** cuelga (probablemente
   llamadas reales a AGROVOC/INTA sin timeout). No investigado a fondo — parecen tests de
   integración dependientes del estado real del Neon o de un extra opcional
   (`sentence_transformers`) no instalado, pero no se confirmó la causa exacta de cada uno.

**Decisión tomada con Sebas (2026-08-13):** separar la mudanza estructural (repo propio + KM por
pip) de la salud del código (estos 4 puntos + los 48 hallazgos de la auditoría de cumplimiento).
La mudanza no requiere que el código esté sano — es organización, no calidad — y mezclar ambas
cosas es el mismo patrón que ya causó retrabajo antes (auditoría corregida dos veces por apurar la
lectura). Se arreglan hoy, antes de mover, solo los bugs concretos y acotados que bloqueaban la
colección de tests (ver más abajo). El resto (22 fallos de `km_tools`, `utils/tests` colgado, y los
48 hallazgos de `AUDITORIA_CUMPLIMIENTO_2026-07-05.md`) queda para una **sesión dedicada aparte**,
no forzada al costado de otra tarea — ver el ítem correspondiente en `agents.md` § Pendiente.
Primer paso sugerido de esa sesión: correr el auditor (`python -m knowledge_module.auditor
--registry auditor_registry.yaml --root .`) para una foto estructurada y actual antes de entrar
hallazgo por hallazgo.

## 5. Foto del auditor corrida hoy (post-fix, antes del move)

`python -m knowledge_module.auditor --registry auditor_registry.yaml --root .` corrió real
contra el Neon de CRIZA: **5 🔴 ALTO, 10 🟡 MEDIO, 46 ⚪ BAJO**.

- **46 BAJO** — casi todos `decision_diferida`: menciones de "v2"/"pendiente"/"TODO" en
  DESIGN_GATEs/ROADMAPs. Poco señal, ruido esperado de documentación de diseño.
- **5 ALTO** — gaps de datos reales en el Neon: `open_access` (0/625 CONICET, 0/1643 INTA),
  `requiere_solicitud` y `solicitud_url` (0/1643 INTA) declarados en la plantilla pero sin poblar;
  + `specialist_proteins.py` sin ninguna escritura al KM detectada (deuda ya conocida y aceptada,
  documentada en el comentario de `auditor_registry.yaml`).
- **10 MEDIO** — 4 son "piezas desconectadas" (`props.mercado`, `props.evidencia`,
  `props.investigacion_amplia`, `props.investigacion_amplia_informe` — producidos, nadie los
  consume, no declarados como salida terminal; el propio auditor lo trackea como contador con
  regla "no debe subir"); 2 son `contrato_input_no_leido` (`armador.py`, `specialist_proteins.py`
  sin `INPUT_CONTRACT` literal declarado); 2 son `km_conexion` (mismos dos archivos sin declarar
  `km_escribe`).
- **2 MEDIO nuevos, causados por la independización de HOY, no deuda vieja:**
  `instancia_no_registrada` sobre `docs/NEW_INSTANCE_PROTOCOL.md` y `docs/platform-boundary.md`
  — el auditor (`knowledge_module/auditor/registry.py:83`, genérico Capa 1) tiene hardcodeado que
  busca esos 2 docs relativos al `--root` recibido, asumiendo que el root es el monorepo
  `EMPRESAS-IA/` (donde sí viven). Con `criza/` como su propio repo y `--root .` = `criza/`,
  genuinamente no están ahí — viven en el otro repo. Mismo patrón que el hallazgo C25 ya
  documentado (herramienta "genérica" de Capa 1 con una ruta de instancia específica cableada),
  visto del otro lado: acá es la herramienta genérica la que tiene cableada una ruta de
  *plataforma*. No se resuelve hoy — es una decisión de diseño del auditor (¿aceptar una ruta al
  repo de plataforma como parámetro? ¿dejarlo como limitación conocida?), le corresponde a la
  sesión dedicada de arriba, no a esta.

Reporte completo (186 líneas) no se adjunta acá — correr el comando de nuevo para verlo entero.

## Pendiente, explícitamente fuera de esta sesión

- Contenerización para aislar instancias entre sí (Docker u otro) — decisión de plataforma,
  necesita su propio Design Gate en `plataforma/`, no se decidió el mecanismo. El trabajo de hoy
  (repo propio + dependencia pip real) es el prerequisito que lo habilita.
- El resto del plan: repo GitHub propio + push (Fase D), move físico de la carpeta (Fase E, lo
  ejecuta Sebas fuera de esta sesión), reflejar la salida en `EMPRESAS-IA/CLAUDE.md` (Fase F),
  verificación post-move (Fase G).
