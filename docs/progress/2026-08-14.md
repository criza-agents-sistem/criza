# Progress — 2026-08-14 — Independización completa + relevamiento + arranque de redefinición de objetivo

## Objetivo de la sesión

Continuar el plan de independización de CRIZA (ver `docs/progress/2026-08-13.md`): ejecutar
Fase D — crear el repo GitHub propio y hacer push — y luego Fase E — move físico de la carpeta.

## Hallazgo — Fase D ya estaba hecha

Al arrancar, sin acceso a Linear en esta sesión (conector `productivity:linear` no autorizado),
se retomó desde `agents.md` + el progress log del 2026-08-13. Antes de ejecutar nada se verificó
el estado real:

- `git remote -v` → `origin` ya apunta a `https://github.com/sebasbizzi/criza.git`
- `gh auth status` → sesión activa como `sebasbizzi`, scopes incluyen `repo`
- `gh repo view sebasbizzi/criza` → el repo existe, **privado**, creado 2026-08-13T16:34:24Z,
  último push 2026-08-13T21:03:31Z
- `git log` local vs `origin/master` → mismo commit (`5d06706`), 0 ahead / 0 behind
- `git status` → working tree limpio

Conclusión: la Fase D se había completado ya en la sesión del 2026-08-13 (probablemente como
parte del trabajo de esa sesión que no quedó explícitamente marcado como cerrado en `agents.md`).
No hubo nada que ejecutar hoy — solo verificar y documentar.

## Hallazgo — Fase E también resuelta

Sebas confirmó haber movido la carpeta a `C:\Users\sebab\Documents\Plataformas\criza`. Se
verificó:

- La sesión ya venía operando en esa ruta (era el cwd desde el arranque de la sesión) —
  confirmado que es hermana de `EMPRESAS-IA\`, mismo patrón que `Conflur\`.
- `EMPRESAS-IA\criza` → no existe (`ls` falla con "No such file or directory") — no quedó
  carpeta vieja ni duplicado.
- `ls Documents\Plataformas\` → lista `criza` como directorio de primer nivel junto a
  `EMPRESAS-IA`, `Conflur`, `Biodarg`, etc.

Conclusión: Fase E cerrada, sin acción adicional necesaria.

## Cambios (Fase D/E)

- `agents.md`: bullets de Fase D y Fase E marcados `[x]` cerrados, con el detalle de
  verificación de cada uno.

## Fase F — reflejar la salida en `EMPRESAS-IA/CLAUDE.md`

La mayor parte ya estaba hecha desde un commit del 2026-08-13 (`3d058db`, "Reflejar la salida de
criza/ del árbol") que actualizó el árbol de "Estructura del proyecto" y "Proyectos hermanos".
Lo que faltaba, hecho hoy en el repo `EMPRESAS-IA` (2 commits, sin push):

- `139c170` — cerrar el bullet de "Pendiente operativo" que todavía decía "falta el Move-Item"
  (ya ejecutado) + cortar la herencia stale de CRIZA en 4 secciones (Protocolo de inicio de
  sesión, Fuentes de verdad, Durante el trabajo, Al cerrar sesión) que todavía instruían leer/
  escribir `criza/agents.md`, `criza/docs/architecture.md`, `criza/docs/progress/...` por path
  relativo, asumiendo que `criza/` seguía nested. Generalizado a "la instancia correspondiente",
  mismo patrón ya usado con `eia1`/Conflur y Biodarg — CRIZA tiene su propio `CLAUDE.md` completo
  desde ayer (commit `5d06706`), no corresponde duplicar ni asumir su estructura interna acá.
- `a80303d` — de paso, se encontró y commiteó trabajo suelto preexistente (no de esta sesión, no
  de CRIZA): un Design Gate completo de **aislamiento por Dev Container por instancia**
  (`plataforma/aislamiento_container/docs/DESIGN_GATE.md`, fechado 2026-08-13, estado 🟡 LISTO
  CON DEUDA) + la regla correspondiente agregada a `.claude/agents/auditor-cumplimiento.md` +
  registro en `layers.yaml` del panel de control. Confirmado con Sebas antes de commitear.

## Fase G — verificación post-move (desde `Plataformas\criza\`)

Los 5 puntos del plan original, todos verificados reales (no mock):

1. **`pip install -e` a la ruta correcta** — `pip show knowledge_module` confirma editable
   install apuntando a `C:\Users\sebab\Documents\Plataformas\EMPRESAS-IA\knowledge_module`
   (ruta absoluta, no se rompió por el move de `criza/`). Documentado el comando exacto en
   `agents.md` (antes tenía un placeholder genérico).
2. **Verificación real del KM** — `motor_api.buscar(area="corpus_cientifico",
   consulta="biotecnologia", tenant="criza", limit=3)` devuelve 3 resultados reales del Neon de
   CRIZA desde la ubicación nueva.
3. **Auditor** — `python -m knowledge_module.auditor --registry auditor_registry.yaml --root .`
   corre sin errores de path desde `Plataformas\criza\` (antes había que cargar `.env` a mano en
   el entorno, el paquete no lee ninguno propio). Resultado: **61 hallazgos (5 ALTO, 10 MEDIO, 46
   BAJO)** — idéntico a la foto del 2026-08-13, sin regresión por el move.
4. **Suite de tests** — corrida por módulo (no en un solo `pytest` global: hay un
   `ImportPathMismatchError` porque varios módulos comparten el nombre de paquete `tests` con
   `__init__.py` propio — problema estructural preexistente de convención de tests, no causado
   por el move, no resuelto hoy). Con `-m "not integration"` (excluye 2 tests que hacen una
   llamada real a Claude y tardan minutos — `test_run_agent_caso_real` en `armador` y
   `evidence_generalista`, ambos marcados `@pytest.mark.integration`):
   - `orquestador/tests`: 28/28 ✅
   - `market_agent/tests`: 67/67 ✅ (+ 6 deselected integration)
   - `evidence_generalista/tests`: 48/48 ✅ (+ 1 deselected)
   - `investigacion_amplia/tests`: 39/39 ✅ (+ 1 deselected)
   - `armador/tests`: 31/31 ✅ (+ 1 deselected) — antes bloqueado por el `NameError` de
     `_CRIZA_DIR`, ya arreglado en el commit `0fa924b` de esta misma sesión previa
   - `scientific_agent/tests`: 110/110 ✅ (+ 32 deselected)
   - `tests/test_ingest.py`: colecta bien (7 tests) — el `IndentationError` documentado el
     2026-08-13 también sigue arreglado
   - `km_tools/tests`: 5/28 verde (era 6/28 el 2026-08-13 — diferencia de 1, dentro del margen
     esperado por depender del estado real del Neon, no investigado — deuda ya conocida, no se
     toca hoy)
   - `utils/tests`: sigue colgando (confirmado con timeout corto) — mismo comportamiento
     documentado el 2026-08-13, deuda ya conocida, no se toca hoy
5. **Documentado acá** — ver este mismo archivo.

**Conclusión de la Fase G: el move no rompió nada.** Todo lo que estaba verde antes del move
sigue verde; todo lo que estaba roto (deuda de `km_tools`/`utils` documentada el 2026-08-13)
sigue roto en la misma magnitud, no peor. Plan de independización de CRIZA (Fases A-G)
**completo**.

## Principio nuevo — "nada corre compartido en runtime" + auditoría de servicios "compartidos"

Sebas aclaró un principio que corrige/endurece el modelo de capas: **ninguna instancia debe
depender de un servicio corriendo una sola vez y compartido por varias** (ni un Modal endpoint,
ni una base de datos, ni datos en disco). El patrón correcto es el mismo que ya se usó con el
KM: la capacidad genérica se **construye una vez como código instalable**, y **cada instancia la
instala/despliega por su cuenta**, de forma aislada — igual que `pip install -e` del KM, no un
servicio central al que todas pegan.

Se pidió auditar `EMPRESAS-IA` en busca de otros candidatos a esta misma ambigüedad. Hallazgos:

- **`services/esmfold/`** — ya estaba detectado desde el **2026-07-06** (`AUDITORIA_CUMPLIMIENTO_
  2026-07-05.md` línea 186, `layers.yaml` línea 22-24): 100% específico del agente científico de
  CRIZA, mal clasificado como Capa Estructural. En su momento la decisión fue corregir solo la
  *etiqueta*, no mover la carpeta física, "para no romper el deploy de Modal ya activo". Sebas
  confirmó ahora que sí se mueve a CRIZA — **pero se decidió esperar** a armar el plan completo
  antes de tocar código (no se ejecutó hoy).
- **`plataforma/document_store/data/criza/`** — hallazgo nuevo, no documentado antes: 1.455 PDFs
  reales de CRIZA huérfanos ahí desde antes de que el código de descarga se mudara al paquete
  `knowledge_module` (2026-07-22). **Resuelto hoy** — ver sección siguiente.
- **`services/bge-m3/`** — hoy es un único deploy de Modal compartido por CRIZA y DPN ("ambas
  migraron juntas", dice la auditoría 2026-07-05). Bajo el principio nuevo debería separarse por
  instancia, pero es un cambio que toca a DPN — **no se tocó hoy**, queda para sesión dedicada.
- **`knowledge_module/connectors/oai_pmh.py`** (harvester OAI-PMH de INTA/CONICET) — la auditoría
  2026-07-05 ya dejó abierta la pregunta de si debería bajar a `criza/` en vez de vivir en el
  paquete genérico. Sigue sin resolver — **no se tocó hoy**.
- **`knowledge_module/plantillas/lecciones.yaml`** — es el único archivo de plantillas que sigue
  en el paquete del KM (`corpus_cientifico.yaml`/`descubrimiento_criza.yaml` ya bajaron a
  `criza/config/plantillas/`). La auditoría 2026-07-05 lo dejó pendiente junto con la separación
  de bases de datos por instancia (P11) — **no se tocó hoy**.

**Decisión de alcance:** de todo lo anterior, solo se resolvió el hallazgo nuevo (PDFs huérfanos).
Los demás (ESMFold, bge-m3, oai_pmh.py, lecciones.yaml) quedan para una sesión dedicada — mismo
criterio que ya rige la auditoría de cumplimiento grande ("no resolver nada de esto sin Sebas,
uno por uno").

## PDFs huérfanos de CRIZA — movidos a `criza/`

- Movidos los 1.455 PDFs de `EMPRESAS-IA/plataforma/document_store/data/criza/` (2,6GB) a
  `criza/document_store_data/criza/` (mismo disco, `mv` directo — no fue una operación de red).
  Verificado: 1.455 archivos en el destino, conteo idéntico.
- `criza/.env` y `.env.example`: agregado `KM_DOCUMENT_STORE_DIR=document_store_data` — antes no
  estaba configurado, así que `knowledge_module.document_store.store` caía al default relativo
  sin apuntar a ningún dato real.
- `criza/.gitignore`: agregada entrada `document_store_data/` (son binarios, no se versionan —
  el texto ya extraído vive en el KM).
- Verificado real (no asumido): `knowledge_module.document_store.store._data_root()` resuelve a
  `criza/document_store_data/` y encuentra los PDFs movidos (`0005faada7c9bf8eba55.pdf` como
  muestra).
- Limpiado en `EMPRESAS-IA`: `plataforma/document_store/__pycache__/` y
  `plataforma/document_store/tests/__pycache__/` — bytecode huérfano de `store.py`/
  `test_store.py`, cuyo código fuente ya no existe ahí (se migró al paquete KM en 2026-07-22).
  Quedó `plataforma/document_store/data/dpn/` (vacía, 0 archivos) sin tocar — es de otra
  instancia, fuera de alcance de esta sesión.
- `agents.md`: actualizada la entrada de "Document Store" en § Stack activo — ya no dice
  `plataforma/document_store/store.py` (Capa 0-1), ahora refleja que es parte del paquete KM
  (Capa 1) con datos propios de CRIZA en su nueva ubicación.

## Ejecución del plan "nada compartido en runtime" — `services/` + `oai_pmh.py`

Plan completo en `C:\Users\sebab\.claude\plans\greedy-cooking-llama.md`, aprobado por Sebas tras
resolver dos dudas en el camino:

- **ESMFold y bge-m3 van a `criza/services/`** — carpeta nueva, hermana de los agentes
  (`market_agent/`, `armador/`, `scientific_agent/`, etc.), no anidada dentro de ninguno. Un
  agente puede conectarse a más de un servicio; ambos ya se llaman solo por URL HTTP, nunca por
  import directo, así que la ubicación del código no depende de quién lo consume.
- **`oai_pmh.py` se mueve a `criza/`** (no se queda en el KM como propuse originalmente) — Sebas
  ajustó el criterio: si algo cumple una función que solo usa CRIZA, va a CRIZA, sin esperar a
  que aparezca en teoría un segundo consumidor.
- **`lecciones.yaml` se confirma sin acción** — se explicó qué es (molde del loop de
  aprendizaje, dos tipos de ficha: `leccion_caso` automática + `leccion_proceso` humana) y se
  comparó con `corpus_cientifico.yaml` (que sí tiene `tenant: criza` hardcodeado pese a decirse
  "reutilizable") — `lecciones.yaml` es el caso que mejor cumple el patrón "código genérico, cada
  instancia aporta su tenant", no hace falta moverlo.

### Cambios ejecutados (verificados reales, no mock)

- **`criza/services/esmfold/`** (nuevo) ← `EMPRESAS-IA/services/esmfold/` — `modal_app.py`,
  `patch_openfold.py`, `README.md`, `docs/DESIGN_GATE.md`. Los mensajes de `scientific_agent/
  tools/esmfold_local.py` que referencian `services/esmfold/...` ya usaban paths relativos
  genéricos — sin cambios de código necesarios ahí.
  Redeploy: `python -m modal deploy services/esmfold/modal_app.py` desde la raíz de `criza/`
  (Windows: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` para evitar `UnicodeEncodeError` con los
  íconos que imprime el CLI de Modal). Mismo nombre de app (`criza-esmfold`) → **misma URL**
  (`criza-dev--criza-esmfold-esmfold-server.modal.run`), cero cambios en
  `criza/scientific_agent/.env`. Verificado con `GET /health` real → `status: ok`.
- **`criza/services/bge-m3/`** (nuevo) ← `EMPRESAS-IA/services/bge-m3/` — mismo patrón, sin
  ningún `add_local_file` (más simple todavía que ESMFold). Redeploy desde `criza/`, misma app
  (`criza-bge-m3`) → misma URL. Verificado con `GET /health` real y una llamada real a
  `POST /embed` (devolvió un vector de 1024 dims). `dpn-normativo/.env` sigue apuntando a la
  misma URL sin tocar — sigue funcionando igual que antes, DPN no se enteró del move.
- **`EMPRESAS-IA/services/`** eliminado por completo (código + `__pycache__` huérfano).
  `layers.yaml`: sacada la entrada `services` de `plataforma:` (ya no queda nada ahí). `CLAUDE.md`:
  sacado `services/` del árbol + la frase "Capa 1 — cómputo compartido en Modal". Agregado un
  bullet nuevo en "Pendiente operativo" documentando el hand-off a DPN (implementar embedder
  en-proceso en su Railway, diseño ya existente en `docs/km-aislamiento-diagnostico.md`
  §12.2-12.5, sesión propia). `docs/KRIZA_Foundation_Document.md`: actualizada la fila de
  embeddings/GPU para reflejar el estado real (antes decía "en transición", ya está listo).
  `AUDITORIA_CUMPLIMIENTO_2026-07-05.md` **no se tocó** — queda como registro histórico fechado.
- **`criza/utils/oai_pmh.py`** (nuevo) ← `knowledge_module/connectors/oai_pmh.py` — código sin
  cambios funcionales, solo docstring actualizado.
- **`criza/ingest/harvest_conicet.py`** (nuevo) ← `knowledge_module/ingesta/ingest_corpus.py` —
  import cambiado a `utils.oai_pmh`, agregado el mismo boilerplate de `sys.path`/`load_dotenv`
  que ya usa `ingest/harvest_inta.py` (antes vivía dentro del paquete ya instalado, no lo
  necesitaba). `criza/ingest/docs/CONICET_CONNECTOR_GATE.md` (nuevo, movido desde
  `knowledge_module/docs/`, con nota agregada aclarando que la sección "Capa: 1" del documento es
  histórica).
- **`knowledge_module`**: borrados `connectors/oai_pmh.py`, `ingesta/ingest_corpus.py` y
  `tests/test_oai_pmh.py` (este último ya estaba roto — hallazgo C25, apuntaba a un path que no
  resolvía desde el repo separado). Confirmado que nada más en el paquete los importaba.
- **`agents.md`**: agregadas las entradas de `services/` y CONICET/`oai_pmh.py` en § Stack
  activo, con el detalle de la migración.

### Verificación

- Import real: `from utils.oai_pmh import OAIPMHHarvester, OAIRecord` — OK.
- `python ingest/harvest_conicet.py --list-sets --config config/connectors/conicet.yaml` — la
  llamada HTTP real se ejecutó, pero **CONICET devolvió 503** (confirmado independiente del
  código: `curl` directo a `ri.conicet.gov.ar/oai/request?verb=Identify` también da 503 — caída
  del servidor externo, no una regresión del move). Import y wiring de config confirmados; la
  cosecha real queda pendiente de que CONICET vuelva a estar arriba.
- `GET /health` real en ambos servicios Modal + `POST /embed` real en bge-m3 — todo verde.

## Fusión de `scientific_agent/` a `criza/` — historial preservado

Sebas: "ese agente debería estar dentro de criza, no tiene sentido que esté afuera, ahora todos
estarán adentro" — `scientific_agent/` era un repo git separado (`github.com/CRIZA-ia/scientific`,
ignorado por `criza/.gitignore`, decisión pendiente desde 2026-07-06 marcada como hallazgos
C5/C16/C17). Se resolvió: se fusiona como carpeta normal de `criza/`, mismo trato que
`market_agent/`, `armador/`, etc.

**Se eligió preservar el historial completo** (26 commits) en vez de solo copiar el estado
actual, vía `git subtree` (no destructivo, no reescribe historia — agrega un merge commit con
dos padres):

1. Se commitearon primero los cambios sueltos que ya tenía ese repo sin confirmar (jubilación
   del scout, migración RunPod→Modal en docs, el `agents.md` que acababa de escribir) — commit
   `b0380c1`, pusheado a `CRIZA-ia/scientific` como resguardo antes de tocar nada más.
2. `git remote add` + `git fetch` del repo nested hacia `criza` (trae todos los objetos/commits
   sin tocar el working tree todavía).
3. Se movió la carpeta física a un backup temporal para liberar el path.
4. Se sacó la línea `scientific_agent/` de `criza/.gitignore` (paso previo obligatorio: `git
   subtree add` exige working tree limpio).
5. `git subtree add --prefix=scientific_agent sciagent-tmp main` — trajo el árbol completo con
   historial. Verificado con `diff -rq --strip-trailing-cr` contra el backup: **contenido
   idéntico**, las únicas diferencias eran archivos gitignoreados (`.env`, `outputs/`,
   `structures/*.pdb`, cachés) y fin de línea (CRLF/LF, cosmético). Historial confirmado con
   `git log <merge-commit>^2` → los 26 commits originales, todos ahí.
6. Copiados a mano los archivos gitignoreados que sí hacen falta para seguir trabajando
   (`.env`, `outputs/`, `structures/`) — el historial de git nunca los tuvo, son datos locales.
7. `pytest scientific_agent/tests -m "not integration"` desde la ubicación nueva → **110/110
   verde**, sin regresión.
8. `scientific_agent/agents.md` (el que había escrito minutos antes) se **eliminó** — ningún
   otro agente de `criza/` tiene su propio `agents.md` (todos usan la tabla central de
   `criza/agents.md` + su propio `ROADMAP.md`); ahora que es una carpeta más del mismo repo, se
   alinea al mismo patrón. `agents.md` (raíz): fila de "Especialista proteínas" actualizada con
   la fusión + los pendientes reales (C16/C17 siguen abiertos, sin resolver hoy).

El repo viejo (`github.com/CRIZA-ia/scientific`) se **archivó** en GitHub (`gh repo archive`,
verificado `isArchived: true`) — queda de solo lectura, disponible como resguardo del historial
original, pero ya no es el lugar donde se trabaja.

## Pendiente

- **DPN sigue apuntando a `criza-bge-m3`** — falta que implemente su embedder en-proceso
  (Railway) y deje de depender de Modal, per diseño ya existente
  (`km-aislamiento-diagnostico.md` §12.2-12.5) — sesión propia de `dpn-normativo`, no de acá
- **Cosecha real de CONICET sin confirmar** — bloqueada por el 503 externo del servidor
  (confirmado con `curl` directo, no es nuestro), no por el move; reintentar cuando el servicio
  esté arriba
- Resto de bloqueadores estructurales sin cambios — ver `agents.md` § Estado operativo
  (auditoría de cumplimiento 51 hallazgos, deuda de tests `km_tools`/`utils`, rotar password
  Neon, renombrar `KRIZA/`→`EMPRESAS-IA/`, C16/C17 de `specialist_proteins.py`)

---

# Segunda mitad de la jornada — arranque de la redefinición de objetivo de CRIZA

Con la independización cerrada (todo lo de arriba), Sebas planteó el cambio de fondo del día:
**CRIZA deja de tener como propósito central "encontrar blue oceans"** — eso ya cumplió su
función, salieron proyectos reales — **y pasa a ser un equipo de agentes de IA asesores**, con
blue-ocean-discovery como una capacidad invocable cuando el caso la amerita, no el propósito que
organiza todo. Esto es un cambio de Norte, no una tarea de código — se trabajó en modo
conversación estratégica, sin tocar el pipeline ni los agentes todavía.

**No está cerrado.** Es la primera sesión de una conversación que sigue. Todo lo acordado y lo
que falta decidir vive en **`docs/PROPUESTA_DESTINO.md`** (documento nuevo, marcado
explícitamente como borrador) — **leer ese documento completo al arrancar la próxima sesión**,
no alcanza con este resumen.

## Cómo se armó

1. **Se encontró que el concepto central ya existía, sin implementar.**
   `EMPRESAS-IA/docs/PROPUESTA_CONDUCTOR.md` (sesión 2026-07-22) ya definía en detalle al
   "Conductor" — el agente conversacional con el que Sebas interactúa principalmente — incluida
   la decisión ya cerrada de "dos puertas de entrada" (Conductor o agente directo), y un
   diagnóstico de por qué el sistema actual no da flexibilidad (costuras no confiables, `tarea`/
   `contexto` descartados — esto último ya se había arreglado el 22/07, verificado hoy).
2. **Se fundamentó todo contra 2 casos reales**, no en abstracto — Sebas creó `casos/` y subió
   el material real:
   - `casos/Efluentes biogas.md` (trae Andrés) — desde cero, transcripción de audios de una
     conversación con Mateo Ligato (Helios Bioenergía) sobre qué hacer con el efluente de
     biodigestor. El propio caso ya viene partido en "frente técnico" y "frente de cómo nos
     asociamos".
   - `casos/MICROBIGS 1.md` + `casos/Propuesta de Alianza...md` + el `.docx.md` del Acuerdo
     Marco (trae Pablo) — producto ya validado (bacterias *Bacillus* para efluentes), necesita
     regulatorio (registro de cepas, Tratado de Nagoya, SAS), estructuración de alianza comercial
     y financiamiento — casi nada de investigación científica de fondo.
   - Hallazgo clave que salió de comparar ambos: **el arquetipo/estadío de un caso no predice
     qué ayuda necesita** — biogás pide ciencia dura, MicroBigs pide abogacía y negocio. Por eso
     el equipo se decidió organizar como **biblioteca de especialistas** (configuración, no
     pipeline fijo), no por estadío.
3. **Se armó un primer borrador de destino** (`docs/PROPUESTA_DESTINO.md`, 11 secciones) y se
   fue iterando en vivo con feedback de Sebas: separación de las dos capas de interacción
   (Sebas-Claude para construir CRIZA vs. Sebas-equipo-CRIZA para asesorar casos), candidatos de
   especialistas ya validados por los casos reales (microbiólogo, ingeniero ambiental, posible
   agrónomo / regulatorio, estructuración de negocio, financiero), y una primera mirada a qué
   pasa con los 4 agentes actuales (ninguno se jubila ni se confirma todavía — quedó como
   discusión abierta, con ideas de hacia dónde podría reorientarse cada uno).
4. **Sebas sumó 4 ideas de producto**, ya incorporadas al borrador:
   - Interacción **web, visual** — confirmado **Next.js** (decisión de plataforma ya tomada en
     `KRIZA_Foundation_Document.md`, nunca construida), no se extiende el panel Jinja2 existente.
   - Elegir **modelo/proveedor de IA por agente** desde un desplegable — se le aclaró un límite
     real: la suscripción (Claude Pro/Max, etc.) no expone API programática, los agentes siempre
     van a correr contra la API paga por uso.
   - Confirmado limpiar el `SYSTEM_PROMPT` de `specialist_proteins.py` (sigue clavado a un caso
     viejo cancelado, hallazgo ya documentado en `PROPUESTA_CONDUCTOR.md` §6) antes de clonar el
     patrón para los especialistas científicos nuevos.
   - **Usuarios con accesos distintos a futuro** — decisión de alcance: diseñar el modelo de
     datos de roles/accesos ahora, sin implementar login todavía (uso actual: solo Sebas).
5. **Se pidieron ideas adicionales para la web**, releyendo los casos con esa pregunta puntual —
   7 ideas surgieron directo del material real (entrada por voz, frentes en paralelo por caso,
   modo documento vs. modo chat, extracción de datos estructurados de la prosa, pendientes
   explícitos por caso, vínculo a artefactos externos, estadío visible del caso) — todas
   confirmadas y sumadas al borrador.

## Estado al cierre

`docs/PROPUESTA_DESTINO.md` tiene 11 secciones, todas marcadas explícitamente como borrador. Lo
más importante para retomar mañana está en su §11 ("Lo que falta decidir antes de tocar
código") — incluye: quiénes usan el sistema además de Sebas, el destino uno-por-uno de los 4
agentes actuales, el registry data-driven, la captura de decisiones, el diseño concreto de la
app Next.js, el modelo de datos de usuarios/roles, y la capa de abstracción de proveedor de
modelo. **Nada de esto se decidió todavía — es la lista de por dónde sigue la conversación.**

## Pendiente operativo para la próxima sesión

- Terminar de revisar `docs/PROPUESTA_DESTINO.md` completo con Sebas (todavía no se hizo una
  pasada de revisión de punta a punta, se fue armando y ampliando en vivo)
- `agents.md` ya tiene un bullet 🔴 al principio de "Estado operativo" apuntando a este
  documento — confirmar que se lea primero en la próxima sesión, antes que cualquier otra cosa
- **Sin decidir todavía: si `casos/` (con datos reales de terceros — nombres, empresas, términos
  de negociación) se commitea al repo privado o se deja fuera de git.** No se commiteó nada de
  `casos/` ni de `docs/PROPUESTA_DESTINO.md` en esta sesión — queda para confirmar con Sebas.
- El Norte global de `CLAUDE.md` ("CRIZA = encontrar blue oceans") sigue sin actualizar a
  propósito — se actualiza recién cuando la conversación de destino cierre, no antes.
