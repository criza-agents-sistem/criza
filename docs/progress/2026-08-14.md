# Progress — 2026-08-14 — Verificación Fases D-G (repo GitHub propio + move físico + Fase F + Fase G)

## Objetivo de la sesión

Continuar el plan de independización de CRIZA (ver `docs/progress/2026-08-13.md`): ejecutar
Fase D — crear el repo GitHub propio y hacer push — y luego Fase E — move físico de la carpeta.

## Hallazgo — Fase D ya estaba hecha

Al arrancar, sin acceso a Linear en esta sesión (conector `productivity:linear` no autorizado),
se retomó desde `agents.md` + el progress log del 2026-08-13. Antes de ejecutar nada se verificó
el estado real:

- `git remote -v` → `origin` ya apunta a `https://github.com/sebasbizzi/criza.git`
- `gh auth status` → sesión activa como `sebasbizzi`, scopes incluyen `repo`
- `gh repo view sebasbizzi/criza` → el repo existe, **privado**, creado 2026-08-13T16:34:24Z,
  último push 2026-08-13T21:03:31Z
- `git log` local vs `origin/master` → mismo commit (`5d06706`), 0 ahead / 0 behind
- `git status` → working tree limpio

Conclusión: la Fase D se había completado ya en la sesión del 2026-08-13 (probablemente como
parte del trabajo de esa sesión que no quedó explícitamente marcado como cerrado en `agents.md`).
No hubo nada que ejecutar hoy — solo verificar y documentar.

## Hallazgo — Fase E también resuelta

Sebas confirmó haber movido la carpeta a `C:\Users\sebab\Documents\Plataformas\criza`. Se
verificó:

- La sesión ya venía operando en esa ruta (era el cwd desde el arranque de la sesión) —
  confirmado que es hermana de `EMPRESAS-IA\`, mismo patrón que `Conflur\`.
- `EMPRESAS-IA\criza` → no existe (`ls` falla con "No such file or directory") — no quedó
  carpeta vieja ni duplicado.
- `ls Documents\Plataformas\` → lista `criza` como directorio de primer nivel junto a
  `EMPRESAS-IA`, `Conflur`, `Biodarg`, etc.

Conclusión: Fase E cerrada, sin acción adicional necesaria.

## Cambios (Fase D/E)

- `agents.md`: bullets de Fase D y Fase E marcados `[x]` cerrados, con el detalle de
  verificación de cada uno.

## Fase F — reflejar la salida en `EMPRESAS-IA/CLAUDE.md`

La mayor parte ya estaba hecha desde un commit del 2026-08-13 (`3d058db`, "Reflejar la salida de
criza/ del árbol") que actualizó el árbol de "Estructura del proyecto" y "Proyectos hermanos".
Lo que faltaba, hecho hoy en el repo `EMPRESAS-IA` (2 commits, sin push):

- `139c170` — cerrar el bullet de "Pendiente operativo" que todavía decía "falta el Move-Item"
  (ya ejecutado) + cortar la herencia stale de CRIZA en 4 secciones (Protocolo de inicio de
  sesión, Fuentes de verdad, Durante el trabajo, Al cerrar sesión) que todavía instruían leer/
  escribir `criza/agents.md`, `criza/docs/architecture.md`, `criza/docs/progress/...` por path
  relativo, asumiendo que `criza/` seguía nested. Generalizado a "la instancia correspondiente",
  mismo patrón ya usado con `eia1`/Conflur y Biodarg — CRIZA tiene su propio `CLAUDE.md` completo
  desde ayer (commit `5d06706`), no corresponde duplicar ni asumir su estructura interna acá.
- `a80303d` — de paso, se encontró y commiteó trabajo suelto preexistente (no de esta sesión, no
  de CRIZA): un Design Gate completo de **aislamiento por Dev Container por instancia**
  (`plataforma/aislamiento_container/docs/DESIGN_GATE.md`, fechado 2026-08-13, estado 🟡 LISTO
  CON DEUDA) + la regla correspondiente agregada a `.claude/agents/auditor-cumplimiento.md` +
  registro en `layers.yaml` del panel de control. Confirmado con Sebas antes de commitear.

## Fase G — verificación post-move (desde `Plataformas\criza\`)

Los 5 puntos del plan original, todos verificados reales (no mock):

1. **`pip install -e` a la ruta correcta** — `pip show knowledge_module` confirma editable
   install apuntando a `C:\Users\sebab\Documents\Plataformas\EMPRESAS-IA\knowledge_module`
   (ruta absoluta, no se rompió por el move de `criza/`). Documentado el comando exacto en
   `agents.md` (antes tenía un placeholder genérico).
2. **Verificación real del KM** — `motor_api.buscar(area="corpus_cientifico",
   consulta="biotecnologia", tenant="criza", limit=3)` devuelve 3 resultados reales del Neon de
   CRIZA desde la ubicación nueva.
3. **Auditor** — `python -m knowledge_module.auditor --registry auditor_registry.yaml --root .`
   corre sin errores de path desde `Plataformas\criza\` (antes había que cargar `.env` a mano en
   el entorno, el paquete no lee ninguno propio). Resultado: **61 hallazgos (5 ALTO, 10 MEDIO, 46
   BAJO)** — idéntico a la foto del 2026-08-13, sin regresión por el move.
4. **Suite de tests** — corrida por módulo (no en un solo `pytest` global: hay un
   `ImportPathMismatchError` porque varios módulos comparten el nombre de paquete `tests` con
   `__init__.py` propio — problema estructural preexistente de convención de tests, no causado
   por el move, no resuelto hoy). Con `-m "not integration"` (excluye 2 tests que hacen una
   llamada real a Claude y tardan minutos — `test_run_agent_caso_real` en `armador` y
   `evidence_generalista`, ambos marcados `@pytest.mark.integration`):
   - `orquestador/tests`: 28/28 ✅
   - `market_agent/tests`: 67/67 ✅ (+ 6 deselected integration)
   - `evidence_generalista/tests`: 48/48 ✅ (+ 1 deselected)
   - `investigacion_amplia/tests`: 39/39 ✅ (+ 1 deselected)
   - `armador/tests`: 31/31 ✅ (+ 1 deselected) — antes bloqueado por el `NameError` de
     `_CRIZA_DIR`, ya arreglado en el commit `0fa924b` de esta misma sesión previa
   - `scientific_agent/tests`: 110/110 ✅ (+ 32 deselected)
   - `tests/test_ingest.py`: colecta bien (7 tests) — el `IndentationError` documentado el
     2026-08-13 también sigue arreglado
   - `km_tools/tests`: 5/28 verde (era 6/28 el 2026-08-13 — diferencia de 1, dentro del margen
     esperado por depender del estado real del Neon, no investigado — deuda ya conocida, no se
     toca hoy)
   - `utils/tests`: sigue colgando (confirmado con timeout corto) — mismo comportamiento
     documentado el 2026-08-13, deuda ya conocida, no se toca hoy
5. **Documentado acá** — ver este mismo archivo.

**Conclusión de la Fase G: el move no rompió nada.** Todo lo que estaba verde antes del move
sigue verde; todo lo que estaba roto (deuda de `km_tools`/`utils` documentada el 2026-08-13)
sigue roto en la misma magnitud, no peor. Plan de independización de CRIZA (Fases A-G)
**completo**.

## Principio nuevo — "nada corre compartido en runtime" + auditoría de servicios "compartidos"

Sebas aclaró un principio que corrige/endurece el modelo de capas: **ninguna instancia debe
depender de un servicio corriendo una sola vez y compartido por varias** (ni un Modal endpoint,
ni una base de datos, ni datos en disco). El patrón correcto es el mismo que ya se usó con el
KM: la capacidad genérica se **construye una vez como código instalable**, y **cada instancia la
instala/despliega por su cuenta**, de forma aislada — igual que `pip install -e` del KM, no un
servicio central al que todas pegan.

Se pidió auditar `EMPRESAS-IA` en busca de otros candidatos a esta misma ambigüedad. Hallazgos:

- **`services/esmfold/`** — ya estaba detectado desde el **2026-07-06** (`AUDITORIA_CUMPLIMIENTO_
  2026-07-05.md` línea 186, `layers.yaml` línea 22-24): 100% específico del agente científico de
  CRIZA, mal clasificado como Capa Estructural. En su momento la decisión fue corregir solo la
  *etiqueta*, no mover la carpeta física, "para no romper el deploy de Modal ya activo". Sebas
  confirmó ahora que sí se mueve a CRIZA — **pero se decidió esperar** a armar el plan completo
  antes de tocar código (no se ejecutó hoy).
- **`plataforma/document_store/data/criza/`** — hallazgo nuevo, no documentado antes: 1.455 PDFs
  reales de CRIZA huérfanos ahí desde antes de que el código de descarga se mudara al paquete
  `knowledge_module` (2026-07-22). **Resuelto hoy** — ver sección siguiente.
- **`services/bge-m3/`** — hoy es un único deploy de Modal compartido por CRIZA y DPN ("ambas
  migraron juntas", dice la auditoría 2026-07-05). Bajo el principio nuevo debería separarse por
  instancia, pero es un cambio que toca a DPN — **no se tocó hoy**, queda para sesión dedicada.
- **`knowledge_module/connectors/oai_pmh.py`** (harvester OAI-PMH de INTA/CONICET) — la auditoría
  2026-07-05 ya dejó abierta la pregunta de si debería bajar a `criza/` en vez de vivir en el
  paquete genérico. Sigue sin resolver — **no se tocó hoy**.
- **`knowledge_module/plantillas/lecciones.yaml`** — es el único archivo de plantillas que sigue
  en el paquete del KM (`corpus_cientifico.yaml`/`descubrimiento_criza.yaml` ya bajaron a
  `criza/config/plantillas/`). La auditoría 2026-07-05 lo dejó pendiente junto con la separación
  de bases de datos por instancia (P11) — **no se tocó hoy**.

**Decisión de alcance:** de todo lo anterior, solo se resolvió el hallazgo nuevo (PDFs huérfanos).
Los demás (ESMFold, bge-m3, oai_pmh.py, lecciones.yaml) quedan para una sesión dedicada — mismo
criterio que ya rige la auditoría de cumplimiento grande ("no resolver nada de esto sin Sebas,
uno por uno").

## PDFs huérfanos de CRIZA — movidos a `criza/`

- Movidos los 1.455 PDFs de `EMPRESAS-IA/plataforma/document_store/data/criza/` (2,6GB) a
  `criza/document_store_data/criza/` (mismo disco, `mv` directo — no fue una operación de red).
  Verificado: 1.455 archivos en el destino, conteo idéntico.
- `criza/.env` y `.env.example`: agregado `KM_DOCUMENT_STORE_DIR=document_store_data` — antes no
  estaba configurado, así que `knowledge_module.document_store.store` caía al default relativo
  sin apuntar a ningún dato real.
- `criza/.gitignore`: agregada entrada `document_store_data/` (son binarios, no se versionan —
  el texto ya extraído vive en el KM).
- Verificado real (no asumido): `knowledge_module.document_store.store._data_root()` resuelve a
  `criza/document_store_data/` y encuentra los PDFs movidos (`0005faada7c9bf8eba55.pdf` como
  muestra).
- Limpiado en `EMPRESAS-IA`: `plataforma/document_store/__pycache__/` y
  `plataforma/document_store/tests/__pycache__/` — bytecode huérfano de `store.py`/
  `test_store.py`, cuyo código fuente ya no existe ahí (se migró al paquete KM en 2026-07-22).
  Quedó `plataforma/document_store/data/dpn/` (vacía, 0 archivos) sin tocar — es de otra
  instancia, fuera de alcance de esta sesión.
- `agents.md`: actualizada la entrada de "Document Store" en § Stack activo — ya no dice
  `plataforma/document_store/store.py` (Capa 0-1), ahora refleja que es parte del paquete KM
  (Capa 1) con datos propios de CRIZA en su nueva ubicación.

## Pendiente

- **Plan de reubicación de ESMFold a CRIZA** — confirmado que se hace, falta armar el plan
  (incluye re-deploy del Modal app desde la nueva ubicación)
- **bge-m3 por instancia** — separar el deploy compartido CRIZA/DPN, toca a DPN, sesión aparte
- **`oai_pmh.py`** — decidir si baja de `knowledge_module/` a `criza/`
- **`lecciones.yaml`** — pendiente junto con separación de bases de datos por instancia (P11)
- Resto de bloqueadores estructurales sin cambios — ver `agents.md` § Estado operativo
  (auditoría de cumplimiento 51 hallazgos, deuda de tests `km_tools`/`utils`, rotar password
  Neon, renombrar `KRIZA/`→`EMPRESAS-IA/`)
- Commits de `EMPRESAS-IA` (`139c170`, `a80303d`) sin push — confirmar con Sebas si corresponde
  pushearlos
