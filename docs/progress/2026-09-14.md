# Progress — 2026-09-14 — Etapa 22: búsqueda genuinamente exhaustiva en el Biotecnólogo

## Objetivo de la sesión

Sebas: "continuemos con los agentes de Helios, ya tenemos información que necesitamos sobre los
efluentes y quiero tener todo el equipo funcionando." Estado real confirmado (`agents.md`,
`docs/progress/2026-09-07.md`): los 6 especialistas ya estaban activos y conectados a
`casos.yaml`, se leen entre sí. La sesión se desvió a un problema nuevo que Sebas trajo en el
medio, más importante que verificar lo ya construido.

## El pedido — productos de redes sociales/revistas especializadas

Sebas ve en Instagram productos de biorefinería/valorización que no van a estar en
OpenAlex/CONICET/INTA por ser internacionales o comerciales, no académicos — preguntó si se podía
armar un agente que revise redes sociales/revistas.

**Explorado con Bright Data** (skills `data-feeds`/`search`/`bright-data-best-practices`):
existen pipelines estructurados reales para Instagram/TikTok/Reddit/YouTube, pero necesitan un
punto de partida (cuenta/hashtag/post puntual) — no hay "buscar por tema en todo Instagram" para
nadie, ni siquiera con la API oficial. Descartadas dos primeras propuestas de Sebas (cuentas
semilla curadas por él — demasiado limitado/sesgado a su idioma; combinar con búsqueda web
genérica primero — demasiado impreciso). Enfoque final acordado: búsqueda técnica multilenguaje
vía SERP de Bright Data + Reddit/YouTube (searchables) + Instagram puntual una vez identificado un
objetivo real. Aclarado explícitamente: **no es un agente nuevo, es sumarle herramientas al
Biotecnólogo existente** (ya tiene el rol de mapear rutas de valorización).

**Bloqueado, no implementado hoy:** necesita que Sebas cree una cuenta de Bright Data (pago) y
consiga una API key + zona SERP — instrucciones exactas ya dadas, pendiente de que lo arme.

## El problema real que se destapó — exhaustividad

En el medio de diseñar esto, Sebas planteó algo más fundamental: **"me preocupa que el agente
decida que no es importante y no busque exhaustivamente... me preocupa el sesgo que tienen a
simplificar y hacer muestreo sin ser exhaustivo."** Verificado contra el código real: no existía
ningún mecanismo que forzara esto — el único precedente (`cobertura_declarada` de
Investigación Amplia) es autodeclarado por el modelo, nadie lo verifica. Coincide directo con
`CLAUDE.md`: "el sesgo se atrapa con estructura/proceso, no con prompts de 'tenelo en cuenta'".

Presentado el tradeoff de costo real explícitamente antes de codear — Sebas: **"la probabilidad
de encontrar aumenta y por ende de éxito, estamos ante un problema que no sabemos si alguien
encontró solución en Argentina nadie, por lo que si no somos exhaustivos el proyecto fracasa y por
ende el resultado del menor gasto en tokens será inútil."** Alcance acordado explícitamente:
empezar por el Biotecnólogo (el caso real de hoy), no los 6 especialistas de una — extender
después con el mismo mecanismo, ya probado.

## Implementado — Etapa 22

- `utils/openalex.py::search_literature_exhaustivo` (nueva) — pagina de verdad en vez de traer
  solo el top-N por relevancia, hasta agotar resultados reales o un techo de seguridad. No
  reemplaza `search_literature` (los otros 3 especialistas la siguen usando igual que siempre).
- `biotecnologo_agent.py`: `search_literature`/`buscar_corpus_cientifico`/`search_corpus_inta` ya
  no dejan que el modelo elija el límite — el código lo fija, siempre.
- **Gate real en código**, no en el prompt: `submit_evaluacion_tecnica` se rechaza si no hubo al
  menos 4 consultas genuinamente distintas en `search_literature` + al menos 1 en cada corpus
  local, con un mensaje explicando exactamente qué falta. Válvula de seguridad de 3 rechazos para
  no quedar en loop infinito si el modelo se traba. `fuentes_y_cobertura` suma
  `gate_exhaustividad`, auditable — mismo criterio que se usó hoy mismo para verificar al
  Ingeniero Ambiental en la sesión anterior.

## 2 bugs reales encontrados con corridas reales (no con análisis a priori)

**Bug 1 — reventó el contexto de Claude.** Primer diseño (`max_total=200` sin truncar abstract,
`limit=2000` en `buscar_corpus_cientifico`, `limit=1000` en `search_corpus_inta`): una corrida
real llegó a **1.5M+ tokens de entrada** (el límite real de Claude es 1M) y falló en el turno 5.
Corregido: `max_total=40` + abstract truncado a 250 caracteres en OpenAlex, `limit=150` fijo en
los corpus locales — números ajustados con dato real medido, no estimados a priori.

**Bug 2 — seguía cerca del límite incluso con los límites bajados.** Con el fix del bug 1, una
corrida real de 8 búsquedas (el doble del mínimo exigido) llegó al **96% del límite de
contexto** (960K/1M tokens) igual — la API de mensajes reenvía la conversación completa en cada
turno, y con búsqueda exhaustiva real eso crece rápido sin importar cuánto se trunque cada
resultado individual. Corregido con **poda de historial**: los `tool_result` de turnos que el
modelo ya "leyó" (el turno donde generó la respuesta siguiente) se reemplazan por un resumen
corto antes de pedir el turno que sigue — nunca el último batch, que el modelo todavía no
procesó. El historial deja de crecer sin límite.

## Verificación real — dos corridas completas, sin tocar el caso real de Helios

Ambas corridas: `_run_loop` directo (no `run_agent_desde_frente`/`invocar_agente`) con una
pregunta técnica real y genérica sobre valorización de digestato/efluente — **no persiste nada al
KM**, deliberado, para no ensuciar el caso real de Helios con un análisis de prueba.

- **1ra corrida** (antes del fix de poda): confirmó el Bug 1 real (falló en el turno 4-5).
- **2da corrida** (fix de límites, sin poda): confirmó el Bug 2 real (96% del límite con 8
  búsquedas, corrida completa igual, cerró bien).
- **3ra corrida** (con la poda ya integrada): **22 turnos reales, 30 variantes de
  `search_literature`** (7x el mínimo exigido — el modelo siguió buscando más allá del piso
  porque el problema lo ameritaba, exactamente el comportamiento que pidió Sebas), más
  KEGG/Rhea/PubChem/ChEBI cruzados en profundidad para confirmar identidad química de los
  candidatos. Ningún turno individual superó 100K tokens de contexto (vs. 960K/1M antes de la
  poda). Duración real: 10.4 minutos. Tokens totales sumados: ~730K (~USD 2-3 de costo real
  estimado por una corrida así de exhaustiva) — costo real, aceptado explícitamente por Sebas de
  antemano.

## Tests y regresión

19 tests nuevos: 6 en `utils/tests/test_openalex.py` (nuevo — paginación real, truncado de
abstract, techo de seguridad), 13 en `biotecnologo_agent/tests/test_biotecnologo_agent.py` (gate
de exhaustividad, poda de historial, dispatch con límites fijos). Regresión completa del repo:
**530 passed**, 34 deselected (no relacionado).

## Estado del commit

Commiteado localmente en la rama `etapa-21-agente-financiero` — **nunca pusheada, sin PR abierto**
(4 commits por delante de `master`, incluido el de hoy). Push/PR pendiente de confirmar con
Sebas, no asumido — no hay upstream configurado para la rama.

## Etapa 22 (cont., más tarde) — Bright Data integrado y verificado + migración a Sonnet 5

**Bright Data — cuenta creada por Sebas, `buscar_web_tecnico` sumado al Biotecnólogo y verificado
real.** `utils/brightdata_serp.py` (paginación real hasta agotar resultados o techo de
seguridad, mismo criterio que `search_literature_exhaustivo`) + integrado a
`biotecnologo_agent.py` con el mismo estándar de exhaustividad de hoy. Verificado con 2 búsquedas
reales en idiomas distintos sobre el mismo tema: inglés (9 resultados: papers, reportes de
mercado) y español (15 resultados distintos, incluyó una asociación española de fabricantes de
fertilizantes — exactamente el tipo de fuente comercial/industria que no aparece en inglés ni en
OpenAlex, confirma el motivo original del pedido). Detalle menor no bloqueante: `total_found`
viene vacío a veces en inglés (Google no siempre devuelve ese campo).

**Corrida completa de punta a punta, 3 fuentes juntas (search_literature + corpus locales +
Bright Data)** sobre un caso técnico real de digestato/efluente porcino (sin persistir al KM,
deliberado, mismo criterio que las corridas de prueba de la mañana): 13 turnos, **23 variantes de
`search_literature`** + 6 de `buscar_web_tecnico` (inglés y español) + KEGG/Rhea/PubChem/ChEBI,
`gate_exhaustividad.cumplido_de_verdad: True`, 582K tokens totales, 619s de duración real. El
agente funcionó bien de punta a punta — el único error fue el script de prueba descartable
crasheando al *imprimir* el informe final por un `UnicodeEncodeError` de la consola de Windows
(cp1252 no puede imprimir '→'), no un bug del agente.

**Migración de modelo default: Sonnet 4.6 → Sonnet 5 en los 11 agentes.** Sebas vio en el
dashboard de uso de Anthropic (Console) que todo corre en Sonnet 4.6 y pidió cambiar a Sonnet 5
("más barato y mejor"). Confirmado con datos reales antes de tocar código: con el volumen del
período (18.5M tokens in / 411K out en 30 días, 100% Sonnet 4.6), Sonnet 5 es la generación
siguiente al mismo precio de tier ($2/$10 por MTok vs $3/$15 de 4.6) — el costo del período baja
~33%, de ~USD 61.70 a ~USD 41.14, sin cambiar de tier. Mejora de calidad en las tareas
específicas de CRIZA: **a-confirmar**, no medida — se ofreció una comparación A/B real y Sebas
priorizó el ahorro de costo ya confirmado. Cambiado: `DEFAULT_MODEL` en los 11 agentes, el
override real de `market_agent/.env`, los 13 `.env.example`, y
`utils/ai_client.py::MODELOS_DISPONIBLES` (Sonnet 5 ahora default de esa lista, Sonnet 4.6 queda
disponible). Decisión registrada en `decisiones_sistema` (KM, componente `ai_client`) +
`docs/architecture.md` actualizado + `agents.md` regenerado. Regresión completa: 674 passed, 1
failed (preexistente, no relacionado — `sentence_transformers` opcional no instalado), 2 skipped.

## Etapa 22 (cont., más tarde 2) — Claude Science/Claude for Life Sciences investigado, search_pubmed sumado

Sebas pidió investigar si Anthropic sumó herramientas nuevas para agentes científicos
(ejemplo dado: anthropic.com/news/claude-science-ai-workbench). Investigación real (WebFetch +
WebSearch, no memoria): **Claude Science es una app de escritorio separada** (beta desde
2026-06-30, macOS/Linux) — coordinador + agentes especialistas por dominio (genómica,
single-cell, proteómica, estructura, cheminformática) + un **agente revisor** que corre en
paralelo y flaguea citas incorrectas/números no trazables/figuras que no coinciden con el
código. No es una librería ni un endpoint de API — no se "instala" en los agentes headless de
CRIZA. Sí expuso una lista de 60+ conectores públicos (Featured connectors) + Skills de
estructura de proteínas (AlphaFold2, Boltz-2, OpenFold3, DiffDock, Evo 2, etc. — algunas corren
contra el servidor público de ColabFold, sin GPU propia) relevantes si se reactiva
`scientific_agent` (hoy inactivo).

Evaluados con evidencia real (no solo lectura de la doc), usando los plugins bio-research ya
instalados en esta sesión de Claude Code: **PubMed** — 94 resultados reales para "digestate
valorization value-added products", vocabulario MeSH que OpenAlex no cubre igual. **ChEMBL** —
0 resultados reales para "struvite" y "phytase", descartado: indexa fármacos/moléculas con datos
clínicos, no el dominio de bioprocesos/enzimas de CRIZA.

**Implementado:** `utils/pubmed.py::search_pubmed` (NCBI E-utilities, sin key requerida, trunca
abstracts a 600 chars — misma lección de contexto de la Etapa 22 original) + tool nueva en
`biotecnologo_agent.py` (12avo tool) y `microbiologo_agent.py` (11avo tool), sin mínimo
obligatorio en el gate de exhaustividad (tratada como precisión de dominio, no pilar exhaustivo).
16 tests nuevos (7 en `utils/tests/test_pubmed.py`, verificado además contra la API real de NCBI
— mismos 94 resultados que el plugin). Regresión completa: 684 passed, 1 failed (preexistente,
no relacionado), 2 skipped. Decisión registrada en `decisiones_sistema` (KM, componente
`biotecnologo`) + `docs/architecture.md` + `agents.md` regenerado.

**Idea de diseño anotada, sin implementar:** el patrón "agente revisor" de Claude Science (chequeo
automático de citas/números antes de persistir) — CRIZA hoy descubre ese tipo de gap a mano
(auditoría de Helios, 2026-08-24). Barato de adoptar (proceso/prompt, no integración nueva) —
pendiente de decidir si vale la pena.

## Etapa 22 (cont., más tarde 3) — cómputo numérico real: utils/estadistica.py

A pedido explícito de Sebas, después de la sesión completa de análisis real del efluente T401 de
Helios (arriba): 4 funciones nuevas sobre series temporales `[{"fecha", "valor"}]` en
`utils/estadistica.py` — `analizar_estabilidad` (media/SD/CV%/tendencia Kendall/clasificación),
`detectar_cambios_de_regimen` (Mann-Whitney entre períodos consecutivos, exige que el cambio se
sostenga varios períodos, filtra blips de un solo punto), `correlacion_con_desfase` (Pearson
barriendo desfase, **siempre** con chequeo de robustez por series diferenciadas — no es opcional),
`comparar_fuentes` (sesgo sistemático entre dos fuentes que deberían medir lo mismo). La lección
más importante del análisis de Helios quedó incorporada como obligatoria en el código: mi
correlación r=-0.71 (ingreso de sustrato vs. conductividad) resultó espuria — el informe
independiente de Claude Science lo confirmó diferenciando las series (r=0.09, no significativa) —
por eso `correlacion_con_desfase` no permite saltear ese chequeo.

Sumadas como 4 tools nuevas en `biotecnologo_agent.py` (16 tools), `microbiologo_agent.py` (15) e
`ingeniero_ambiental_agent.py` (10) — `agronomo_agent.py` deliberadamente excluido, sin necesidad
real visible todavía. `pandas`/`numpy`/`scipy` sumados a `requirements.txt` (no estaban
declarados, solo instalados globalmente en el entorno de desarrollo).

**Tests:** 16 unitarios con series sintéticas de propiedad conocida (estable real, tendencia real,
quiebre real vs. blip filtrado, correlación real con desfase vs. espuria por tendencia compartida,
fuentes comparables vs. sesgo sistemático). **Verificado real, no solo mocks:** dispatch real (sin
mockear la función) en los 3 agentes da el mismo resultado con la misma serie; y una corrida real
de chat/consulta libre del Biotecnólogo (llamada real a la API) ante una pregunta en lenguaje
natural sobre estabilidad de FOS/TAC hizo que el modelo llamara solo, sin indicación de qué tool
usar, `analizar_estabilidad_serie` y `detectar_cambios_de_regimen` — y dio una interpretación de
dominio correcta, identificando el mismo outlier (feb-2025) que después confirmó el informe
independiente de Claude Science sobre el caso real. Regresión completa del repo: 712 passed, 1
failed (preexistente, no relacionado), 2 skipped. Decisión registrada en `decisiones_sistema`
(KM, componente `biotecnologo`) + `docs/architecture.md` + `agents.md` regenerado.

## Pendiente

- **Traer el informe de Claude Science (T401 unificado) al caso de Helios** como
  `documento_aportado` — discutido, no ejecutado todavía (Sebas puede querer revisar el mío
  primero o consolidar ambos).
- **Idea sin decidir: "agente revisor" de citas/números** (ver arriba) — inspirado en Claude
  Science, no en CRIZA todavía.
- **Extender el gate de exhaustividad a los otros 5 especialistas** — acordado explícitamente
  empezar acotado por el Biotecnólogo hoy; el mecanismo (paginación real, límites fijos en
  código, poda de historial) ya está probado y listo para reusar, pero no se tocó ningún otro
  agente todavía.
- **Comparación A/B real Sonnet 4.6 vs Sonnet 5** en una tarea de CRIZA — costo ya confirmado, calidad no medida (ver arriba).
- **Push/PR de la rama `etapa-21-agente-financiero`** — decisión de Sebas, no tomada hoy.
- Pendientes de sesiones anteriores sin tocar: [SEB-277](https://linear.app/sebabizzi/issue/SEB-277)
  (títulos genéricos de `documento_caso`), diseño del especialista Regulatorio para MicroBigs
  (identificado el 2026-08-24, sin arrancar).
