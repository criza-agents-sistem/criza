# Progress — 2026-08-15 — Revisión de PROPUESTA_DESTINO.md + registry data-driven y la costura

## Objetivo de la sesión

Retomar la redefinición de objetivo de CRIZA arrancada el 14/08 (`docs/PROPUESTA_DESTINO.md`).
Sebas había dejado comentarios en un doc paralelo (`docs/PROPUESTA_DESTINO_CRIZA_2.0.md`,
mayúsculas para marcar sus notas) — se procesaron todos, y de ahí surgió un orden de trabajo
para 4 ítems técnicos pendientes: **1) registry data-driven + costura de persistencia al KM,
2) captura de decisiones como eventos, 3) diseño de la app Next.js, 4) modelo de datos de
usuarios/roles.** Esta sesión cubre el ítem 1 completo.

## Comentarios de Sebas procesados (antes de tocar código)

- **§6 (Evidence Generalista, rol pre-filtro vs. post-síntesis):** de acuerdo con Sebas — Evidence
  Generalista es technology-agnostic, no tiene el conocimiento de dominio para triage previo;
  tiene más sentido usarlo para arbitrar entre especialistas después. Queda anotado, a validar
  con un caso real, no se construye todavía.
- **§7 (análisis de datos):** confirmado sumar como punto 8 de la lista de ideas para la web —
  es función (tool de cálculo real) y modo de interacción (tabla/gráfico) a la vez.
- **§10 (costuras rotas):** confirmado que el hallazgo central de `PROPUESTA_CONDUCTOR.md`
  (persistencia depende de que cada agente se acuerde) seguía sin garantía estructural — se sumó
  al análisis de hoy para resolverlo de raíz, no parche por parche.
- **§11 (rol del Conductor):** confirmado que el Conductor asesora sobre qué especialistas hacen
  falta, pero no codea — construir sigue siendo tarea de Sebas + Claude Code. No cambia la
  secuencia de `PROPUESTA_CONDUCTOR.md`.
- Orden de los 4 ítems técnicos: elegido por dependencia (registry+costura → captura de
  decisiones → app Next.js → usuarios/roles), confirmado por Sebas.

## Ítem 1 — Registry data-driven + la costura (completo)

### El problema real, verificado contra el código (no en abstracto)

- `orquestador/registry.py` tenía 4 imports hardcodeados a mano, con `sys.path` manipulado y un
  hack `sys.modules.pop("tools", None)` para que `market_agent/tools/` no colisionara con el
  `tools` de otro agente en el mismo proceso.
- Cada agente decidía por su cuenta si y dónde escribía al KM — la causa exacta del bug del
  22/07 (Mercado corría, costaba plata, y su escritura vivía en el runner, invisible para el
  Motor). Se había parchado ese caso puntual, pero sin garantía estructural para agentes nuevos.
- Revisando los 4 agentes uno por uno se encontraron **dos gaps reales, no solo un riesgo
  teórico**: **Armador nunca había persistido su propio expediente** — vivía solo en memoria
  durante la corrida (`motor.py::_extract_expediente` lo leía de `steps_output`, nunca del KM).
  **Investigación Amplia duplicaba su informe en dos props** (`investigacion_amplia` con
  `informe_completo` adentro, y además `investigacion_amplia_informe` por separado con el mismo
  texto) — ya estaba marcado como "pieza desconectada" por el auditor.

### Diseño (confirmado con Sebas antes de tocar los 4 agentes)

- **`orquestador/agents_registry.yaml`** (nuevo) — registro central data-driven: nombre, módulo,
  descripción, `km.prop_key` por agente. `cientifico_especialista` queda `activo: false` (C16/C17
  sin resolver, no expone `run()` estándar).
- **`orquestador/registry.py`** (reescrito) — lee el YAML, `importlib.import_module` por agente,
  sin imports hardcodeados. `AgentSpec` (dataclass) en vez de `{nombre: callable}` plano.
- **`orquestador/invocador.py`** (nuevo) — `invocar_agente(spec, contract_input, tenant,
  oportunidad_id, verbose)`: corre el agente y persiste `props[prop_key] = output["análisis"]`
  de forma genérica, siempre — sin casos especiales por agente. Es "la costura" de
  `PROPUESTA_CONDUCTOR.md` §4.2: la persistencia deja de ser responsabilidad del agente. Tiene un
  placeholder explícito (`_registrar_evento`) para el ítem 2 de la lista de hoy.
- **Contrato normalizado**: el `análisis` que devuelve `run()` de cualquier agente pasa a incluir
  siempre `informe_completo` junto a sus campos estructurados — es exactamente lo que la costura
  persiste, sin transformación. Convención documentada para cualquier especialista futuro.
- **Fix de raíz del choque de `tools/`**: `market_agent/`, `evidence_generalista/`, `armador/`,
  `scientific_agent/` ganaron `__init__.py` (son paquetes reales ahora). `market_agent.py` pasó
  de `from tools import (...)` a `from market_agent.tools import (...)` — ya no depende de que
  su carpeta esté agregada a `sys.path` a mano. `investigacion_amplia/` ya tenía `__init__.py`.

### Cambios en los 4 agentes (uno por uno, verificado con tests después de cada uno)

- **Mercado**: sacado el `actualizar_props` manual de `run_agent()`. `run()` devuelve
  `análisis = {**cruces, "informe_completo": resumen}` (antes era `{"resumen":..., "cruces":...}`,
  shape distinto al que ya se escribía al KM — inconsistencia interna cerrada). `run.py` pasa a
  invocar vía la costura (antes llamaba `run_agent()` directo).
- **Evidence Generalista**: sacado el `actualizar_props` manual. `run()` devuelve `análisis =
  evidencia` directo (`evidencia_dict` ya tenía `informe_completo` adentro, exactamente lo que
  Armador ya esperaba leer de `props.evidencia` — sin cambios en el lado consumidor). `run.py`
  actualizado.
- **Investigación Amplia**: sacado el `actualizar_props` manual (incluida la escritura duplicada
  de `_informe`). `run()` devuelve `análisis = resultado` directo. El bloque CLI (`if __name__ ==
  "__main__"`) actualizado para pasar por la costura.
- **Armador**: `análisis = {**expediente, "informe_completo": resumen}` — **primera vez que
  `props.armador` existe**. `motor.py::_extract_expediente` actualizado para priorizar
  `informe_completo`, con fallback a las claves viejas (`expediente`/`informe`) por compatibilidad.
  `run.py`: modo 2 (oportunidad real) pasa por la costura; modo 1 (caso de ejemplo sin KM) sigue
  llamando `run_agent()` directo — nunca tuvo `oportunidad_id`, nunca persistía, no hay nada que
  cambiar ahí.

### Verificación

- **Tests**: corrida por módulo tras cada cambio, más una corrida combinada final de los 5
  componentes afectados — **218/218 verde** (`orquestador` 33, `market_agent` 67, `evidence_
  generalista` 48, `investigacion_amplia` 39, `armador` 31). Sumado `orquestador/tests/
  test_invocador.py` (nuevo, 5 tests) — la costura no tenía cobertura directa antes.
- **De paso, arreglada una fragilidad preexistente** (no introducida hoy): varios tests usaban
  `asyncio.get_event_loop().run_until_complete(...)`, un patrón deprecado que fallaba con
  `RuntimeError: no current event loop` según el orden de colección de pytest — se hizo visible
  al agregar `test_invocador.py` a la carpeta. Reemplazado por `asyncio.run(...)` en
  `test_motor.py` (8 ocurrencias) y `test_investigacion_amplia.py` (11 ocurrencias).
- **Real, no mock**: creada una oportunidad real en el Neon de CRIZA, invocada la costura con un
  agente de prueba, releído `props` desde la base — confirmado que lo que se escribe es
  exactamente lo que se relee.
- **Auditor**: antes 61 hallazgos (5 ALTO/10 MEDIO/46 BAJO), después 63 (5 ALTO/9 MEDIO/49 BAJO)
  — MEDIO bajó en 1 (la pieza desconectada de investigación amplia ya no existe), BAJO subió en 3
  (ruido esperado, comentarios nuevos con palabras como "pendiente"). En el camino apareció un
  ALTO nuevo (`km_conexion` en investigación amplia — su `OUTPUT_CONTRACT.km_escribe` seguía
  declarando la clave vieja duplicada) — corregido antes de cerrar, confirmado que desapareció.

## Ítem 2 — Captura de decisiones como eventos (Caso A completo, Caso B anotado para después)

Al retomar el ítem 2 apareció una ambigüedad real en "captura de decisiones": había dos cosas
distintas mezcladas bajo el mismo nombre. Se las separó con Sebas antes de construir nada:

- **Caso A — decisiones de sistema** (arquitectura/desarrollo de CRIZA, la capa Sebas+Claude
  construyendo CRIZA, no la capa de asesoramiento de casos): por qué `agents.md` se desactualiza
  sesión tras sesión pese a la regla de DoD que dice "actualizar `agents.md`" — la regla existía,
  pero apoyada en prosa a mano, sin estructura que la sostuviera. **Decisión de Sebas: ir por todo,
  no a medias** ("quiero que el sistema tenga una buena base de funcionamiento y minimizar errores
  silenciosos") — se construyó completo hoy, ver detalle abajo.
- **Caso B — decisiones de caso** (lo que el futuro Conductor necesita leer de un caso concreto
  cuando Sebas le hace una pregunta): Sebas identificó que estar bien estructurado en el KM no
  alcanza — lo que importa es el protocolo de lectura/retrieval del Conductor cuando "se despierta"
  o responde sobre un caso puntual. **Diferido a propósito** — no hay Conductor todavía a quien
  engancharle ese protocolo, diseñarlo aislado sería adivinar la forma final. Queda anotado como
  requisito explícito en `docs/PROPUESTA_DESTINO.md` §11 para cuando se diseñe el Conductor, no
  se pierde por no construirse hoy.

### Caso A — qué se construyó

1. **Área KM nueva `decisiones_sistema`** (`config/plantillas/decisiones_sistema.yaml`, cargada
   real en el Neon de CRIZA — `area_id d306d1fa-ceb2-4376-adc6-12d647d31cfe`). Campos: `componente`,
   `titulo`, `decision`, `alternativas_consideradas`, `motivo`, `quien`, `estado`
   (`vigente`/`superada`), `fecha`, `texto_busqueda`. Decisiones se **superan, no se editan
   in-place** — una decisión nueva con `supera_id` marca la vieja `superada` y queda conectada
   (`tipo_conexion: supera`), como el resto del KM (ninguna decisión "desaparece", solo deja de
   estar vigente).
2. **`scripts/km_decisiones.py`** — `registrar_decision(...)` y `listar_decisiones_vigentes(...)`.
   Verificado real: escritura + relectura contra Neon.
3. **`scripts/generar_agents_md.py`** — regenera "Agentes activos" (lee `agents_registry.yaml` +
   corre la suite de tests real de cada agente, no `--collect-only` — correr los tests es la única
   forma de saber si de verdad pasan + última decisión vigente del componente) y "Estado
   operativo" (todas las decisiones vigentes que no son de un agente específico — reemplaza los
   ~100 líneas de bullets a mano que existían antes). Reemplaza solo el contenido entre
   marcadores `<!-- GENERADO:...:INICIO/FIN -->`, el resto de `agents.md` queda intacto.
4. **Migración de los pendientes abiertos** (`scripts/_migrar_decisiones_iniciales.py`, corrida
   única) — los bullets `[ ]` (abiertos) de la vieja sección "Estado operativo" se registraron como
   decisiones reales antes de regenerar, para no perder nada: redefinición del objetivo de CRIZA,
   `objetivo` decorativo del Motor, el Auditor determinístico, deuda de tests, rotar password Neon,
   renombrar carpeta `EMPRESAS-IA/`, auditoría de cumplimiento. Los bullets `[x]` (ya cerrados —
   Fases D-G, reestructuración del KM) **no se migraron a propósito**: ya cumplieron su función de
   avisar que algo estaba incierto, y su detalle completo sigue íntegro en `docs/progress/*.md` —
   migrarlos como "vigente" solo habría vuelto a bloatear la sección generada.
5. **`agents.md` regenerado de punta a punta**: se sacó el banner viejo "rethink 2026-06-13"
   (superado varias veces, ya no aportaba nada — su contenido relevante se resumió en una frase
   dentro de "Qué es CRIZA"). Las filas "Motor v2" y "Auditor" salieron de la tabla "Agentes
   activos" (no son agentes) — su narrativa vive ahora en las decisiones `componente=orquestador`
   y `componente=auditor` de "Estado operativo" generado, sin duplicar. 414 líneas → 318 (todavía
   arriba del límite de ~200 declarado en el propio archivo, pero la reducción real está en que las
   dos secciones que se desactualizaban dejaron de ser prosa a mano — el resto del archivo, fuera
   de alcance de hoy, es contenido más estable).
6. **`CLAUDE.md` y `agents.md` § REGLAS OPERATIVAS — regla de DoD actualizada**: de "actualizar
   `agents.md` a mano" a "si la tarea generó una decisión de arquitectura → registrarla en
   `decisiones_sistema` + correr el generador". Las dos secciones generadas ya no se editan a mano.

### Alcance explícitamente recortado (avisado, no silencioso)

- **"Knowledge Module — estado rápido"** (la tercera tabla candidata a generarse) se dejó **sin
  tocar** — el generador ya tiene la función lista (`componente=knowledge_module`), pero esa tabla
  es un inventario de capacidades estables (qué existe, qué versión), no una lista de decisiones
  pendientes con alternativas — forzar sus ~14 filas históricas al formato de decisión habría sido
  encajar contenido en la forma equivocada. Queda listo para cuando haga falta (agregar el
  marcador a `agents.md` + sumar `KM_ESTADO` a `_MARCADORES_ACTIVOS` en el generador).
- **`.env` no se cargaba en `scripts/km_decisiones.py`** — el primer intento de migración falló con
  `ModuleNotFoundError: sentence_transformers` porque sin `EMBEDDING_PROVIDER=bgem3` (viene del
  `.env` de la raíz) el motor caía al embedder local, que no está instalado acá. Fix: `load_dotenv`
  agregado al módulo, mismo patrón que usan los 4 agentes.

### Verificación

- **218/218 unit tests verde** (misma suite que el ítem 1, sin regresión).
- **Auditor: 63 hallazgos** (5 ALTO/9 MEDIO/49 BAJO) — idéntico al número post-ítem-1, sin
  hallazgos nuevos introducidos por el trabajo de hoy.
- **Real, no mock**: 8 decisiones migradas + releídas desde Neon: `python -c
  "listar_decisiones_vigentes()"` devolvió las 8; se generó `agents.md` con ellas; se detectó y
  retiró una decisión de prueba (`componente=test`, de la verificación de la plantilla) que había
  quedado vigente y estaba filtrando al archivo generado — vuelta a generar, confirmado que
  desapareció.

## Ítem 4 — Modelo de datos de usuarios/roles (hecho, sin `participa_en` todavía)

Elegido antes que el ítem 3 (diseño de la app) por dependencia: el modelo de datos de caso que
va a salir del ítem 3 necesita saber desde el arranque quién es dueño de qué, para no tener que
retrofitearlo después.

**Investigación previa (antes de escribir nada):** revisado el schema real del KM. La tabla
`oportunidad` de `schema.sql` (la que describe la fila "Knowledge Module — estado rápido" de
`agents.md`, sin tocar hoy) es del agente convergente/divergente **ya borrado** — muerta. El
nodo real de "caso" que usa el sistema hoy es `ficha` de tipo `oportunidad` en área
`descubrimiento` (motor genérico, migración `005_tipo_ficha_oportunidad.sql`), creado por el
Motor al arrancar un pipeline — muy flaco (`nombre`/`descripcion`), sabor a descubrimiento. Y
confirmado contra el KM real: **no existe ninguna ficha para los casos reales** (MicroBigs,
Biogás) — viven solo como markdown en `casos/`. Las 3 fichas `oportunidad` que sí existen son de
corridas viejas de discovery (metano entérico, ganadería bovina) más una de prueba de hoy.

**Diseño propuesto y confirmado con Sebas:** área `usuarios` con `tipo_ficha usuario` (nombre,
email, rol_global, estado, notas) + `tipo_ficha rol` (catálogo abierto) + una conexión tipada
`participa_en` (usuario → cualquier ficha de caso) con `rol_en_caso`.

**Ajuste durante la construcción (encontrado, no anticipado, avisado antes de seguir):** el
loader del motor genérico (`knowledge_module/motor/loader.py::load_plantilla`) valida que
`desde`/`hacia` de una `tipo_conexion` sean nombres de `tipo_ficha` **declarados en la misma
plantilla** — no hay forma de declarar "conecta con cualquier ficha" ni de apuntar a un tipo de
otra área (`oportunidad` vive en la plantilla de `descubrimiento`). Inventar acá un `tipo_ficha
caso` provisorio solo para poder conectar hoy habría creado un segundo concepto de "caso"
compitiendo con lo que va a definir el ítem 3. **Decisión: construir `usuario`+`rol` completo
hoy, dejar `participa_en` para cuando el ítem 3 declare la ficha real de caso** — se agrega en
minutos en ese momento, no hay que rediseñar nada de lo de hoy.

**Construido y verificado real:**
- `config/plantillas/usuarios.yaml` — cargada en el Neon de CRIZA (`area_id
  2901041d-3991-4951-bbf8-a6ab67f73a4c`).
- Catálogo de roles sembrado: `admin`, `colaborador`, `referente`, `observador`.
- 3 usuarios reales sembrados: **Sebas** (`admin`, `activo`, con email) — **Pablo** (`referente`,
  `invitado`, sin email — dueño de MicroBigs) — **Andrés** (`referente`, `invitado`, sin email —
  dueño del caso de efluentes de biogás). Releídos desde Neon, confirmado que lo escrito es lo
  que se relee.
- Decisión registrada en `decisiones_sistema` (`componente=usuarios`) y `agents.md`
  regenerado — aparece en "Estado operativo".
- **218/218 tests, auditor sin cambios (63 hallazgos)** — sin regresión, ninguna de las dos
  cosas de hoy toca código de agentes.

## Pendiente

- **Caso B (protocolo de lectura del Conductor)** — anotado en `docs/PROPUESTA_DESTINO.md` §11,
  se diseña junto con el Conductor, no antes.
- **Conexión `participa_en` (usuario → caso)** — depende de que el ítem 3 declare la ficha real
  de "caso". No se puede construir antes por la restricción del loader explicada arriba.
- Ítem 3 (diseño de la app Next.js) — el único que queda de la lista original de 4.
- **`agents.md` sigue arriba del límite de ~200 líneas** — el resto de secciones (Stack activo,
  Dónde están las cosas, Convención, REGLAS OPERATIVAS, KM estado rápido) no se tocó hoy, fuera de
  alcance del ítem 2.
- `armador/armador.py` sigue sin `OUTPUT_CONTRACT`/`INPUT_CONTRACT` declarado literalmente
  (deuda preexistente, no de hoy — ya estaba marcada por el auditor).
- C16/C17 de `specialist_proteins.py` — sin resolver, `cientifico_especialista` sigue `activo:
  false` en el registro nuevo.
- Resto de bloqueadores estructurales sin cambios — ver `agents.md` § Estado operativo (ahora
  generado, refleja el estado real).
