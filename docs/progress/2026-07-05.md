# Progress — 2026-07-05/06 — Auditoría de cumplimiento de plataforma

## Qué se hizo

Auditoría de cumplimiento pedida por Sebas: verificar que la Capa 0-1 y todas las instancias
cumplen el playbook y el diseño previsto, sin muestreo. Se hizo en dos pasadas:

1. **Pasada 1 (docs/estructura):** lectura de los documentos canónicos (playbook, platform-boundary,
   DESIGN_GATE_TEMPLATE, NEW_INSTANCE_PROTOCOL) + 3 subagentes en paralelo auditando dpn-normativo,
   eia1 y la capa de plataforma (código real) + revisión directa de CRIZA a nivel de Design
   Gates/agents.md/estructura de carpetas.
2. **Pasada 2 (código real de CRIZA):** Sebas señaló que "el diseño y desarrollo de este sistema
   de agentes fue cambiando, por lo que hay cosas que están en CRIZA y son transversales" — un
   subagente leyó línea por línea los 5 agentes + `utils/` + `ingest/` buscando específicamente
   código genérico atrapado en Capa 2. Confirmado: `openalex.py` duplicado, `token_tracker.py`
   referenciado por un principio de plataforma pese a vivir en Capa 2, `inta.py` reimplementando
   el harvester OAI-PMH que ya existe genérico en `knowledge_module/`, y el loop de aprendizaje
   roto en el camino real de producción (Motor v2 nunca persiste lecciones).

Total: 47 hallazgos (32 de la pasada 1 + 15 de la pasada 2), 24 de ellos propios de CRIZA (C1-C24).

## Qué se construyó

- `docs/AUDITORIA_CUMPLIMIENTO_2026-07-05.md` (raíz) — reporte consolidado con evidencia
  archivo:línea de los 47 hallazgos.
- `.claude/agents/auditor-cumplimiento.md` + `knowledge_module/docs/CUMPLIMIENTO_PLAYBOOK_GATE.md`
  — agente reusable de auditoría (Capa 1), complementario al auditor determinístico existente.
- `plataforma/control_panel/` — panel web local (FastAPI, probado corriendo y con 10/10 tests
  pasando) con 4 vistas: conjunto, por capas, auditoría (estado editable), alta de instancia nueva.
- 42 de los 47 hallazgos cargados como issues en Linear (SEB-234 a SEB-275, prefijo `[AUDIT-*]`).

## Decisiones tomadas

- El agente auditor nuevo es **complementario**, no reemplaza al determinístico
  (`knowledge_module/auditor/`) — este juzga contenido/coherencia, aquel verifica reglas fijas.
- El panel de control usa HTML server-rendered en vez de Next.js — priorizar tener algo corriendo
  hoy sobre pureza de stack (desviación documentada en su Design Gate).
- Ningún hallazgo se resolvió unilateralmente — todos quedan para revisar con Sebas.

## Qué quedó pendiente

- **Revisar los 47 hallazgos con Sebas** y decidir método de resolución por caso (prompt a la
  instancia / edición directa / decisión documentada) — es la próxima sesión.
- 5 hallazgos (C16, C17, C18, C19, C24) sin issue en Linear — se alcanzó el límite del plan
  gratuito del workspace a mitad de la carga. Crear a mano o tras upgradear el plan.
- `criza/agents.md` sigue por encima de su propio límite de ~200 líneas (314 hoy) — no se
  reestructuró en esta sesión para no mezclar ese trabajo con la auditoría; es en sí mismo un
  hallazgo menor a agregar a la lista.

## Qué problemas encontramos (proceso)

- Un subagente lanzado para la pasada 2 intentó delegar el trabajo a otro subagente en vez de
  hacerlo él mismo — se corrigió reenviándole instrucción explícita de no delegar. Vale tenerlo
  en cuenta si se vuelve a usar el patrón de subagentes en paralelo para auditorías futuras.
- Linear cortó la carga de issues a mitad de camino por límite de plan — no se puede completar
  el registro de hallazgos en Linear sin resolver eso primero.
