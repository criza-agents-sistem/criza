# Sesión 2026-06-14 — SEB-150: Conector CONICET OAI-PMH → KM

Continuación desde 2026-06-13 (rethink cerrado, diseño completo). Esta sesión construyó
el primer módulo de la secuencia CONICET → Mercado → Armador.

## Lo hecho

### SEB-157 (Design Gate CONICET) → Done
Gate completo en `knowledge_module/docs/CONICET_CONNECTOR_GATE.md` — 5 decisiones cerradas,
estado 🟡 LISTO CON DEUDA. Creado en la sesión anterior (resumen de contexto).

### SEB-150 (Conector CONICET OAI-PMH → KM) → Done ✅
14/14 tests pasando. Archivos creados:

| Archivo | Capa | Qué es |
|---|---|---|
| `knowledge_module/plantillas/corpus_cientifico.yaml` | 1 | Área corpus + tipo_ficha `fuente` |
| `knowledge_module/connectors/__init__.py` | 1 | Módulo connectors |
| `knowledge_module/connectors/oai_pmh.py` | 1 | Harvester OAI-PMH genérico (cosecha incremental, parser DC, resumption tokens, reintentos) |
| `knowledge_module/ingesta/__init__.py` | 1 | Módulo ingesta |
| `knowledge_module/ingesta/ingest_corpus.py` | 1 | Ingesta end-to-end: harvest → filtro → dedup exacto → `guardar_ficha` → Neon. CLI con `--list-sets`, `--from`, `--until`, `--limit` |
| `criza/config/connectors/conicet.yaml` | 2 | Config CONICET: endpoint, sets, keywords de pertinencia, parámetros HTTP |
| `knowledge_module/tests/test_oai_pmh.py` | — | 12 unit + 2 integration |

**Decisiones de implementación:**
- Dedup por `identifier` exacto manejado en `ingest_corpus.py` (no en el motor) — el motor actual solo soporta dedup semántico; no se tocó.
- `titulo_abstract` = campo calculado (titulo + ". " + abstract) enviado como campo `fuente` y declarado como `vectorizar` en la plantilla. Motor sin cambios.
- Estado incremental guardado en `conicet_harvest_state.json` junto al YAML de config (Capa 2, fuera de git).
- Test de idempotencia: verifica ausencia de `identifier` duplicado en Neon (no que `ingeridos == 0` — OAI-PMH no garantiza orden consistente entre requests).
- `requests` + `pyyaml` agregados a `requirements.txt`.

## Estado

- SEB-150 **Done** ✅ — corpus científico operativo, listo para ser leído por los investigadores vía `buscar()` / `vecinos()`.
- CONICET endpoint verificado en vivo: `https://ri.conicet.gov.ar/oai/request` responde correctamente.
- El harvester genérico está listo para ser reusado por SEB-151 (INTA) y por DPN.

## Próximo

**Secuencia: SEB-148 (Mercado repotenciar)**
- Primer agente investigador de la nueva arquitectura
- Lee del corpus vía búsqueda semántica (`buscar`)
- Design Gate antes de codear (norma del proyecto)
- Sugiero abrir el gate de SEB-148 como próxima sesión
